---
title: "Themes"
---

```{r}
#| include: false
library(tabviz)
```

Built-in theme presets, the `web_theme()` custom-theme constructor, the four core
modifiers (`set_brand()`, `set_accent()`, `set_density()`, `set_header_style()`),
and `package_themes()` for listing every shipped theme. Pass any theme to
`tabviz(theme = ...)` or apply it after the fact with
[`set_theme()`](styling.qmd#set_theme).

For the conceptual model see the [Themes guide](../guide/themes.qmd) and the
[Styling Cascade](../concepts/styling-cascade.qmd) concepts page.

---

## Preset themes

`web_theme_*()` builders return a fully resolved `WebTheme`. They take no
arguments — chain modifiers to customize. Nine presets ship, each owning a
distinct expressive axis: the **clinical** default `nejm`, two **editorial**
voices, two **design** movements, and four **expressive** moods.

| Theme | Register | Identity |
|---|---|---|
| [`web_theme_nejm()`](#web_theme_nejm)         | clinical | The package default — restraint-led: teal + warm-coral accent, Inter, comfortable |
| [`web_theme_ledger()`](#web_theme_ledger)       | design | Color: teal + oxblood, ruled rules |
| [`web_theme_brutalist()`](#web_theme_brutalist) | design | Geometry: sharp corners, thick rules, no effects |
| [`web_theme_aurora()`](#web_theme_aurora)       | expressive | Effects: glass + glow on a dark canvas |
| [`web_theme_terminal()`](#web_theme_terminal)   | expressive | Mono phosphor CRT |
| [`web_theme_newsprint()`](#web_theme_newsprint) | editorial | Texture: warm grain, serif body |
| [`web_theme_blueprint()`](#web_theme_blueprint) | expressive | Draft / grid: cyanotype |
| [`web_theme_synthwave()`](#web_theme_synthwave) | expressive | Neon |
| [`web_theme_dwarven()`](#web_theme_dwarven)     | editorial | Fantasy: Cinzel display serif |

```r
tabviz(data, label = "study",
       columns = list(viz_forest(point = "hr", lower = "lo", upper = "hi")),
       theme = web_theme_nejm())
```

The 9 committed identities — each owns one expressive axis:

### `web_theme_nejm()` {#web_theme_nejm}
### `web_theme_ledger()` {#web_theme_ledger}
### `web_theme_brutalist()` {#web_theme_brutalist}
### `web_theme_aurora()` {#web_theme_aurora}
### `web_theme_terminal()` {#web_theme_terminal}
### `web_theme_newsprint()` {#web_theme_newsprint}
### `web_theme_blueprint()` {#web_theme_blueprint}
### `web_theme_synthwave()` {#web_theme_synthwave}
### `web_theme_dwarven()` {#web_theme_dwarven}

All preset builders take no arguments and return a fully resolved `WebTheme`.
`nejm` is the default. (A previous 27-preset roster was culled to these 9; a
deleted look's brand is a [`set_brand()`](#set_brand) recolor away, its full
personality a `web_theme(…)` custom build away.)

---

## `package_themes()` {#package_themes}

Returns a 2-level named list of every theme distributed with the package,
grouped by register (`clinical` / `editorial` / `design` / `expressive`) —
handy for the `enable_themes` argument of
[`web_interaction()`](interaction.qmd) or for iterating in tests. The
nested shape is preserved by the in-widget switcher when you pass it
through, producing a tabbed picker (one tab per category).

```r
package_themes() |> str(max.level = 2)
# List of 4
#  $ clinical  :List of 1
#   ..$ nejm     : <WebTheme>
#  $ editorial :List of 2
#   ..$ newsprint: <WebTheme>
#   ..$ dwarven  : <WebTheme>
#  $ design    :List of 2
#   ..$ ledger   : <WebTheme>
#   ..$ brutalist: <WebTheme>
#  $ expressive:List of 4
#   ..$ aurora   : <WebTheme>
#   ..$ terminal : <WebTheme>
#   ..$ blueprint: <WebTheme>
#   ..$ synthwave: <WebTheme>
```

---

## `web_theme()` {#web_theme}

Custom-theme constructor. Pass Tier-1 inputs as direct arguments; everything
else derives. Resolves the theme before returning.

```r
web_theme(
  brand = "#0099CC",          # required: identity seed
  accent = NULL,              # engagement (hover/selected/callouts); mirrors brand
  polarity = "light",         # "light" | "dark" (L-reflection)
  monochrome = FALSE,         # neutral ramp rides the brand hue (B8)
  categorical = "okabe_ito",  # data palette scheme
  sequential = "viridis",
  diverging = "rdbu",
  status_positive = NULL, status_negative = NULL,
  status_warning = NULL,  status_info = NULL,
  fonts_body = NULL, fonts_display = NULL, fonts_mono = NULL,
  density = "comfortable",    # "compact" | "comfortable" | "spacious"
  header_style = "light",     # "light" | "tint" | "bold"
  first_column_style = "default",  # "default" | "bold"
  web_fonts = NULL,
  name = "custom"
)
```

| Argument | Type | Default | Description |
|---|---|---|---|
| `brand` | hex | `DEFAULT_BRAND_HEX` | Required identity seed. Powers the brand ramp and identity tokens. |
| `accent` | hex | `NULL` | Engagement layer (hover, selected, callouts). Mirrors `brand` when unset. |
| `paper` / `ink` | hex / oklch | `NULL` | Light-end and dark-end neutral anchors; the neutral ramp interpolates between them. |
| `marks` | list | `NULL` | Viz-mark identity: `point_shape`, `interval_weight`. |
| `effects` | list | `NULL` | Glow / gradient shell / elevation / glass / caption, header, title styles. |
| `mode` | string | `"standard"` | `"standard"` / `"high-contrast"` / `"reduced-transparency"` — accessibility, orthogonal to polarity. |
| `polarity` / `monochrome` | string / flag | `"light"` / `FALSE` | L-reflection axis; brand-hued neutral ramp. Use [tint_from_brand()] to nudge hues instead. |
| `categorical` / `sequential` / `diverging` | string | scheme names | Data-palette scheme references. |
| `status_*` | hex | `NULL` | Optional status color overrides (`positive`/`negative`/`warning`/`info`). |
| `fonts_body` / `fonts_display` / `fonts_mono` | string | `NULL` | Font stacks; `fonts_display` mirrors `fonts_body` when unset. |
| `density` | string | `"comfortable"` | `"compact"` / `"comfortable"` / `"spacious"`. |
| `header_style` | string | `"light"` | Header chrome variant: `"light"` / `"tint"` / `"bold"`. |
| `first_column_style` | string | `"default"` | Label-column variant: `"default"` / `"bold"`. |
| `web_fonts` | list | `NULL` | Optional `web_font()` declarations to embed. |
| `name` | string | `"custom"` | Theme name (shown in the in-widget switcher and in source export). |

To start from a preset instead of defaults, chain setters off a `web_theme_*()`
builder (e.g. `web_theme_nejm() |> set_brand("#1F3A5F")`).

Example — navy editorial theme with bold headers:

```r
custom <- web_theme(
  name = "navy-bold",
  brand        = "#1F3A5F",
  accent       = "#B08938",
  fonts_body    = "Charter, Georgia, serif",
  density      = "comfortable",
  header_style = "bold"
)
```

---

## Theme modifiers

All modifiers operate on a `WebTheme` and return a new (re-resolved) `WebTheme`.
Chainable via the pipe.

### `set_inputs()` {#set_inputs}

Batch-update Tier-1 inputs and re-resolve. Accepts any [`ThemeInputs`] field by
name; cascade-aware (derived tokens refresh, explicit pins survive). For a
single input prefer the focused setter (`set_brand()`, `set_accent()`,
`set_density()`, …); use `set_inputs()` to change several at once.

```r
set_inputs(
  theme,
  brand, accent, paper, ink, polarity, monochrome, mode,
  categorical, sequential, diverging,
  status_positive, status_negative, status_warning, status_info,
  fonts_body, fonts_display, fonts_mono,
  density
)
```

| Argument | Type | Description |
|---|---|---|
| `theme` | `WebTheme` | Theme to modify |
| `brand` | hex | Identity seed (markers, identity tokens). Derives the brand ramp |
| `accent` | hex | Engagement layer — hover, selected, semantic callouts, selection edge. Mirrors `brand` when unset |
| `paper` / `ink` | hex / oklch | Light-end / dark-end neutral anchors for the neutral ramp |
| `mode` | `"standard"` / `"high-contrast"` / `"reduced-transparency"` | Accessibility mode |
| `polarity` / `monochrome` | string / flag | L-reflection axis; brand-hued neutrals |
| `categorical` / `sequential` / `diverging` | string | Data-palette scheme names |
| `status_positive`, `status_negative`, `status_warning`, `status_info` | hex | Semantic status colors |
| `fonts_body` | string | Primary CSS font-family |
| `fonts_display` | string | Optional title face; mirrors `fonts_body` when unset |
| `fonts_mono` | string | Optional monospace face |
| `density` | string | `"compact"` / `"comfortable"` / `"spacious"` |

> Identity vocabulary: `brand` is the **input** name; `primary` / `primary_deep`
> are the **resolved** surface tokens it derives (along with secondary and the
> per-series slot anchors). To pin a resolved token directly — e.g. a specific
> series fill — use `set_theme_field(c("series", 1L, "fill"), "#…")`.

### Variant setters {#set_variants}

Single-purpose fluent setters (the former bundled `set_variants()` was
retired in favor of one setter per knob, matching `set_brand()` /
`set_accent()` / `set_density()`).

```r
set_density(theme, density)               # re-resolves the cascade
set_header_style(theme, header_style)     # variant selector (assign-only)
set_first_column_style(theme, first_column_style)
```

| Setter | Values | Description |
|---|---|---|
| `set_density()` | `"compact"` / `"comfortable"` / `"spacious"` | Tier-1 input — re-resolves and drives every spacing token |
| `set_header_style()` | `"light"` / `"tint"` / `"bold"` | Post-resolution variant: light header on muted surface, primary-tinted band, or bold header on `primary_deep` |
| `set_first_column_style()` | `"default"` / `"bold"` | Post-resolution variant: emphasized first (label) column |

### `set_spacing()` {#set_spacing}

Per-token spacing override on top of the active density preset. Set what you
care about; the rest cascade from density.

```r
set_spacing(
  theme,
  row_height, header_height, padding, container_padding, axis_gap,
  column_group_padding, row_group_padding, cell_padding_x,
  footer_gap, title_subtitle_gap, bottom_margin, indent_per_level
)
```

All values are numeric (pixels).

::: {.callout-note}
## Live aspect-driven `row_height` in the widget

When the in-widget [aspect-ratio slider](../guide/interactivity.qmd#aspect-ratio-slider) is engaged, the lever ladder scales `row_height` to absorb the height delta. The Spacing settings tab reads the **effective** (post-lever-ladder) value, banner-flagged "Row height (aspect)". Editing the field clears the aspect target so the user-typed value pins. See [Themes guide — aspect-aware spacing](../guide/themes.qmd#aspect-aware-spacing).

Other tokens (`header_height`, gaps, paddings) keep the value `set_spacing()` writes; only `row_height` is live-driven by the slider.
:::

### `set_theme_field()` {#set_theme_field}

Generic deep setter. Walks the theme tree by `path` and replaces the leaf
with `value`. Assigning under `inputs` re-resolves; other paths pin directly.

```r
set_theme_field(theme, path, value)
```

| Argument | Description |
|---|---|
| `theme` | A `WebTheme` |
| `path` | Character or mixed character/integer vector. Strings are property names; integers index list-typed properties (currently only `series`). |
| `value` | New value for the leaf field |

Examples:

```r
# Pin row-group L1 background regardless of theme
theme |> set_theme_field(c("row_group", "L1", "bg"), "#EEEEEE")

# Override series 2's stroke without touching anything else
theme |> set_theme_field(c("series", 2L, "stroke"), "#444444")

# Set Tier 1 inputs surgically
theme |> set_theme_field(c("inputs", "brand"), "#FF0000")

# Tweak axis config
theme |> set_theme_field(c("axis", "gridlines"), TRUE)
theme |> set_theme_field(c("axis", "tick_count"), 5)

# Pin layout config
theme |> set_theme_field(c("layout", "container_border_radius"), 4)
```

Re-resolution preserves explicit overrides on subsequent edits.
`set_inputs(brand = "...")` after pinning `surface.base` will refresh
series colors but leave `surface.base` pinned.

---

## Switching themes on a spec

| Verb | Operates on | Purpose |
|---|---|---|
| [`set_theme()`](styling.qmd#set_theme) | `WebSpec`, htmlwidget, proxy | Swap to a preset name or `WebTheme` object |
| [`selectable_themes()`](styling.qmd) | `WebSpec`, htmlwidget | Curate the in-widget theme switcher |

```r
# Apply by name
plot |> set_theme("nejm")

# Apply by object
custom <- web_theme(brand = "#1F3A5F")
plot |> set_theme(custom)
```

---

## Worked examples

### Editorial theme with custom Tier 1 input

```r
my_editorial <- web_theme(
  name = "my-editorial",
  brand        = "#0066CC",
  accent       = "#CC9900",
  fonts_body    = "Helvetica, Arial, sans-serif",
  fonts_display = "Helvetica, Arial, sans-serif",
  density      = "comfortable"
)

# Swap presets, then tweak
custom <- web_theme_nejm() |>
  set_brand("#0066CC") |>
  set_density("compact") |>
  set_theme_field(c("axis", "gridlines"), TRUE)
```

### High-contrast print theme

```r
print_theme <- web_theme(
  name = "print",
  brand        = "#000000",
  accent       = "#000000",
  fonts_body    = "'Times New Roman', serif",
  fonts_display = "'Times New Roman', serif",
  density      = "compact"
) |>
  set_theme_field(c("row_group", "L1", "text", "weight"), 700) |>
  set_theme_field(c("row_group", "L1", "border_bottom"), TRUE)
```

### Dark dashboard theme

```r
dark_theme <- web_theme(
  name = "dashboard-dark",
  polarity     = "dark",
  brand        = "#10B981",
  accent       = "#10B981",
  density      = "spacious",
  header_style = "bold"
)
```

---

## Theme structure (S7)

A `WebTheme` is an S7 object with the following top-level properties.
For the conceptual model and how the cascades compose, see
[Styling Cascade](../concepts/styling-cascade.qmd) and
[S7 Architecture](../technical/s7-classes.qmd).

| Property | Type | Tier | Description |
|---|---|---|---|
| `name` | string | — | Theme identifier |
| `inputs` | `ThemeInputs` | 1 | Customer-facing inputs |
| `variants` | `ThemeVariants` | 1 | Per-table toggles |
| `surface` | `Surfaces` | 2 | base / muted / raised |
| `content` | `Content` | 2 | primary / secondary / muted / inverse |
| `divider` | `Dividers` | 2 | subtle / strong |
| `accent` | `AccentRoles` | 2 | default / muted / tint_subtle / tint_medium |
| `status` | `StatusColors` | 2 | positive / negative / warning / info |
| `series` | `list<SlotBundle>` | 2 | Per-anchor 7-field bundle |
| `summary` | `SlotBundle` | 2 | Pooled-effect diamond bundle |
| `text` | `TextRoles` | 2 | 8 named role bundles |
| `spacing` | `SpacingTokens` | 2 | 12 density-derived numerics |
| `annotation` | `AnnotationCluster` | 3 | title / subtitle / caption / footnote |
| `header` | `HeaderCluster` | 3 | light + bold variants |
| `column_group` | `ColumnGroupCluster` | 3 | Column-group header |
| `row_group` | `RowGroupCluster` | 3 | L1 / L2 / L3 tiers |
| `row` | `RowCluster` | 3 | base / alt / hover / selected + the five semantic bundles (`emphasis`, `muted`, `accent`, `bold`, `fill`) |
| `cell` | `CellCluster` | 3 | bg / fg / border / text |
| `first_column` | `FirstColumnCluster` | 3 | plain + bold variants |
| `plot` | `PlotScaffold` | 3 | axis / tick / gridline / reference + mark sizes |
| `marks` | `MarksRecipes` | 3 | Per-mark slot-bundle recipes |
| `axis` | `AxisConfig` | config | Forest x-axis range/ticks/grid |
| `layout` | `Layout` | config | Plot width, container border |
