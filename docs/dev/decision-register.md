# Decision register

Status: **living ledger** (stood up 2026-06-10). The tidy-keeping half of
`docs/dev/ship-roadmap.md`.

**The rule:** any work that defers a product decision MUST add an entry
here instead of deferring silently. Every entry carries a
**default-if-undecided** — what ships if nobody decides — and a
**decide-by** milestone. At each milestone review, entries due are decided
(moved to the Decided section with a one-line rationale) or explicitly
re-dated. The register must be empty of pre-1.0 rows at ship.

Entry format: `ID | question | options | default if undecided | decide by`.

## Open

| ID | Question | Options | Default if undecided | Decide by |
|----|----------|---------|----------------------|-----------|
| D40 | Scientific-notation exponents (pvalue `6.0×10⁻⁴`) use Unicode superscript glyphs (⁻⁴). In the SVG **export**, librsvg substitutes Courier for MONO themes (terminal, synthwave), which renders ⁻⁴ with a gap + wrong size (`10⁻ 4`). Serif/sans themes render fine (7/9 presets). Fix = render exponents as proper SVG superscript (`baseline-shift` + smaller `<tspan>`, regular digits) in a shared render-tree superscript node — works in every font. (a) build the render-tree superscript node + reroute the pvalue/scientific renderers (fixes DOM+export universally); (b) export-only `kind:"svg"` markup in the pvalue svg renderer; (c) leave the Unicode superscripts (legible, just gapped on 2 mono themes). | (c) — output stays legible; the fix is cross-cutting for a 2-theme edge case | M2 (pre-1.0 polish) |


## Decided

| ID | Decision | Rationale | Date |
|----|----------|-----------|------|
| D38 | **Decided KEEP hand-rolled (option b) — the perf gate ran and rejects delegation (2026-06-18).** Benchmarked the gating question (does a per-column V8 round-trip hurt?): hand-rolled `serialize_column` = 0.066 ms/col; a `ts_call` V8 round-trip = 0.736 ms/col → **11.2× slower, +13.4 ms on a 20-col spec**. That's a real regression on a hot path (Shiny re-serializes every reactive update; `split_table` serializes many specs; serialize perf is a flagged sensitivity). Per-column delegation (option a) is out. A BATCHED single-round-trip (option c) could amortize the overhead but is a non-trivial refactor that ALSO couples the serialize path to V8 (load cost; not always needed for pure-data serialize) — for a MARGINAL benefit, since `test-parity-columns.R` (34 tests) already catches R↔TS column-wire drift cheaply. So 'drift impossible by construction' buys little over 'drift caught by tests' while costing perf + coupling. Keep the fast, safe, tested hand-rolled path. | The perf measurement (the explicit gate) shows delegation is 11× slower for a benefit the parity tests already deliver. Re-open only if drift becomes a recurring real problem — then revisit the batched variant. | 2026-06-18 |
| D39 | **Decided KEEP component-kind (option a) — the fork is INTENTIONAL DOM richness, NOT debt (2026-06-18).** Investigated whether `bar`/`heatmap` could convert to `kind:"svg"` (the round-2 tier-2 suggestion). They can't without degrading the DOM: `CellBar.svelte` uses `display:flex` + `.bar-track{flex:1}` + `.bar-fill{transition:width 0.2s}` (dynamic flex layout + an animated fill); `CellHeatmap.svelte` uses flex centering + dynamic `style:background-color`. A static SVG fundamentally can't do flex/transitions — the `*-svg-renderer.ts` is the faithful static-export APPROXIMATION (it pre-computes the label width via measureTextWidth precisely because the DOM flex row grows it). `CellPictogram` is additionally interactive. So component-kind on the DOM + svg-renderer on export is the SAME necessary pattern as the declared browser-only WYSIWYG breaks (glass/glow/blobs) — DOM richness that can't be statically exported, with `glyph-cell-parity` guarding they stay visually close. Converting would make the interactive DOM WORSE to chase a phantom 'fork'. | The 'fork' is the DOM being richer than the static export, by design — not duplicated logic. Unifying to svg-kind trades real DOM layout/animation for a static approximation. Reject option (b). | 2026-06-18 |
| D37 | **SUBSTANTIALLY RESOLVED via geometry-sharing; the `drawMark` capstone DESCOPED as unnecessary (2026-06-18).** Steps 1–4 landed: all divergence-prone forest-mark + viz geometry (shape point-math — star/triangle/diamond/marker; band-split layouts — bar/boxplot/violin floors+ratios+the 2px gap) now has ONE shared source (`lib/annotation-glyph.ts`, `lib/forest-mark-geometry.ts`, `lib/viz-mark-geometry.ts`), called by BOTH the DOM components and `svg-generator.ts`. Two real DOM↔export divergences reconciled (summary-diamond apex-clamp; violin sub-floor maxWidth). Each step byte-identical (validated by layout-metrics, which transfers to the DOM since both call the same helper). The `drawMark`→RenderNode capstone (step 6) is DESCOPED: once the geometry is shared, the residual twin code is trivial `xScale()` positioning or the necessary per-runtime emission shell (Svelte vs strings) — the capstone is high-risk-on-the-hottest-code, low-marginal-bug-prevention gold-plating. Full record + re-open criteria: docs/dev/d37-forest-mark-unification.md. | The goal was killing duplicated LOGIC (round-1 bugs lived in both copies); the geometry was that logic and it's now unified. The capstone changes structure (not just sharing), so it lacks the layout-metrics safety net the geometry steps had — not worth the regression risk for purity. | 2026-06-18 |
| D34 | **PREMISE DISPROVEN — `CustomAnnotation` renders fine; no gating. The `# TODO: Frontend rendering not implemented` comment was STALE; deleted (2026-06-18).** A round-1 R-auditor declared the feature a no-op by reading only the R side (TODO + serialize) and never checking TS — but BOTH paths render the glyph: export `svg-generator.ts` (~5020, circle/square/triangle/star, the path round-1's Infinity fix touched) and DOM `TabvizPlot.svelte:2200-2217`. Confirmed end-to-end: rendering `inst/examples/gallery_07_annotations.R` (`forest_annotation(shape="star")`) through the V8+rsvg export emits `<polygon>` glyphs. Gating a working public feature behind an experimental "may not render" warning would have been a regression. The `#8b5cf6` default is a legitimate glyph color, not a bug. | Verify-before-acting caught a false premise (the 3rd bad agent claim this thread, after theme-diff + the inflated studio LOC) — the feature works and is gallery-demonstrated; the only real defect was a stale comment. | 2026-06-18 |
| D29 | **Ratified default (a) — the panel stays curated; the 8 uncovered Group-A inputs stay R-only via web_theme()/set_*.** The redesign's layer taxonomy (D21) was deliberate and the panel optimizes the common 80%; add sibling controls only on demand. Full audit: docs/dev/settings-coverage-gaps.md. | Defaults ratified at the maintainer's roadmap review; documented decide-late items accepted as-is. | 2026-06-18 |
| D25 | **Ratified default (a) — ship with density preset + density_factor only; no per-token spacing verb.** Those two cover the common case; per-token spacing is expert-grade and R/set_spacing() already serves it. A sanctioned setSpacingOverride verb stays a post-1.0 on-demand item. | Defaults ratified at the maintainer's roadmap review; documented decide-late items accepted as-is. | 2026-06-18 |
| D20 | **Ratified — NO pre-1.0 label-column sheds.** The 2026-06-17 blast-radius analysis settled it: chrome/sugar/LABEL_MAX are JUSTIFIED keeps; #7 already removed; the only controversial sheds (#1 labelColumn-vs-columns[0], #2 Row.label slot, #8 figureLayout slot) are wire-major → post-1.0. The label column is less 'on thin ice' than feared; revisit the wire sheds in a post-1.0 major. | Defaults ratified at the maintainer's roadmap review; documented decide-late items accepted as-is. | 2026-06-18 |
| D35 | **Ratified default (a) — formatPvalue p>1 passes through honestly.** A p slightly over 1 from rounding is plausible and showing it is honest; clamping/hiding could mask a data error. NaN/±Inf/negative already → naText. Low stakes; revisit only on user confusion signal. | Defaults ratified at the maintainer's roadmap review; documented decide-late items accepted as-is. | 2026-06-18 |
| D36 | **Option (b) reframed — KEEP the studio as a LIVING TUTORIAL (maintainer, 2026-06-18, superseding the same-day (c) decision).** Don't delete it and don't salvage its cascade viz into the settings panel: the tier-by-tier cascade-teaching spine (`components/theme-panel/cascade/`) is too bulky to fold into a collapsible settings pane, so the studio STAYS as its home — a maintained teaching/tutorial surface, not dormant dead-weight. It keeps shipping (the 938 KB bundle is now a feature, not waste), keeps its smoke gate (`studio-shot.mjs`), and `tabviz_studio()` stays exported. The "do not extend" guidance narrows to "don't add *editing* features here — the live settings panel owns editing; do keep the tutorial functional." `docs/dev/d36-studio-salvage-plan.md` is SUPERSEDED (kept as the inventory record + the rejected salvage analysis). | The studio's cascade pedagogy is genuinely vision-aligned (rgc-design "teaching-by-default") and has no equivalent in the task-oriented live panel; the collapsible settings-pane format can't host the bulky spine viz. So the value is real AND can't move — keeping the studio as a living tutorial preserves it at the cost of one extra (justified) bundle. Reverses the (c) delete-eventually framing. | 2026-06-18 |
| D33 | **Option (c) — FULL TS alignment, the buffer fudge ELIMINATED, 2026-06-16.** `.inject_systemfonts_widths` (`R/save_plot.R`) had hard-coded `rendering_buffer=8`, `auto_min=40`, `auto_max=480` (one cap for all). Resolved all four to the TS source EXACTLY: buffer 8→4 (`RENDERING_BUFFER`), min 40→60 (`AUTO_WIDTH.MIN`), per-column cap — data 480→600 (`AUTO_WIDTH.MAX`), label column → 400 (`AUTO_WIDTH.LABEL_MAX`, the DOM caps the label there, svg-generator.ts:432/526). The min/max realign is truncation-SAFE by construction (raising only widens / prevents truncation; the label 480→400 matches the screen's existing cap). The +8 buffer was the one fudge candidate — it was ASSUMED to compensate for systemfonts measuring narrower than the estimator, but an eyes-on render check settled it empirically: `save_plot` PNGs of long clinical labels ("Metoprolol succinate extended-release", "Composite cardiovascular and renal outcome") at buffer=4 truncate NOTHING and overflow NOTHING — so the +4 was unjustified drift, not real compensation. Aligning removes the divergence entirely (no fudge). Anti-recurrence: a new source-parse test (`test-systemfonts-injection.R`) pins the R constants to the TS values, wired into the in-tree CI sync-gate step. The stale CLAUDE.md "systemfonts narrower → one-pad tolerance" trap (which had justified the fudge) corrected. | option (a)/(b) kept a fudge; the render check proved buffer=4 sufficient, so full alignment is the principled, fudge-free answer — and it improves WYSIWYG (the screen and the save_plot export now share identical width clamps) | 2026-06-16 |
| D32 | **Investigated + closed 2026-06-16 — there was never a second flex IMPLEMENTATION; one engine, three callers, spec-building now DRY'd.** The original entry's premise ("the DOM store `columns.svelte.ts` has its OWN width path, no `resolveFlexWidths`") was WRONG: `columns.svelte.ts` only MEASURES content naturals — the DOM's *distribution* lives in `layout-zoom.svelte.ts` and calls `resolveFlexWidths`, the SAME engine the export (`svg-generator.ts`, 2 sites) uses. So all three render-width sites already shared one engine. The genuine duplication was the `ColumnWidthSpec` ASSEMBLY (3 near-identical copies of `naturalWidth: explicit ?? measured ?? viz ?? fallback` + flexWeight/minWidth/cap) → extracted to a shared `toColumnWidthSpec` (`flex-weights.ts`) that locks the spec shape + natural-resolution order. The CONTEXT-specific parts stay per-caller because they're legitimately different: the live store sources explicit/measured from store state + targets the CONTAINER (compress); the export sources from the wire + (from-scratch) grows to natural because it has NO container. That from-scratch divergence (the wysiwyg-gate budgets) is INHERENT, and production PINS the widget's measured widths into the export (D20 item 4) so it's invisible in real use. No risky live-layout "unification" needed or done; byte-identical (layout-metrics + wysiwyg + hero + interaction-qa all unchanged). | the engine was already centralized; the only real brittleness (spec assembly) is now shared, and the remaining divergence is by-design context, not a second implementation | 2026-06-16 |
| D31 | **Wired the 3 real interaction flags + DELETED `enable_select` — option (a), 2026-06-16.** `show_legend` / `enable_collapse` / `enable_hover` were documented R API + serialized + in the roster with baked-ON defaults but had ZERO behavior consumers (legend always drew, groups always collapsed, hover always highlighted); `enable_select` gated a row-selection feature that does not exist. Per D9 ("opt out per-spec/theme/global"), these three ARE the promised opt-outs, so honoring the docs = wiring them. Gated (all default-ON, only explicit FALSE disables): the DOM legend `{#if}` + the export `renderLegend` early-return (showLegend); the group-header toggle handlers + the GroupHeader chevron/aria (enableCollapse); `handleCellEnter`'s hover-effect call — NOT the paint-preview, the precise gate (enableHover). `enable_select` removed from R (param/property/3 presets/serializer/`TABVIZ_INTERACTION_FLAGS`), TS (`INTERACTION_FLAG_KEYS`/baked/type/authoring), + tests; the R↔TS roster-sync gate confirms both sides match. Verified: interaction-qa green (collapse/paint/export still work default-ON), +resolve opt-out + enableSelect-gone tests. | (a) was the register default — honor the documented behavior as the D9 opt-outs; `enable_select` had no feature, so delete the dead knob. Low-risk, no regression. | 2026-06-16 |
| D30 | **The "exacting users" variant tier is option (a) — recipe primitives exposed as kind-tagged options, 2026-06-16.** Promoted `interval`'s 7 hidden bounds knobs to real `presentation` `OptionSpec`s (`boundsLayout`, `boundsContent`, `boundsOpen`, `boundsClose`, `boundsSeparator`, `boundsPrefix`, `boundsMuted`) — all default null ⇒ defer to the selected `variant`; an explicitly-set primitive OVERRIDES it (`recipeFor` overlays author primitives over the variant's `__resolved`). Precedence theme-variant < author-variant < author-primitive holds; the primitives are themeable (`themeable:TRUE`, set via `set_column_default(th, "interval", boundsSeparator="/")`) so a house style can fine-tune too. Harmonizes interval with badge (variant cards over exposed primitives). Wired R (`col_interval(bounds_*=)`), TS (`colInterval`), schema + regenerated artifacts, introspection (`column_schema("interval")` lists all 7). Verified: 4 TS override tests, R parity round-trip + default-absent, end-to-end export render ("bracket_muted but with /" → `[0.80/1.01]`), `column_schema` reach gate. Option (b) (a `VariantSpec`-authoring API for custom NAMED recipes) stays the post-1.0 extension arc, on demand — it overlaps custom-column-type registration and R has no surface for it yet. | (a) was the register default; cheap, fully governed through the existing column_defaults pipeline, and makes interval consistent with badge | 2026-06-16 |
| D27 | **Default heatmap palette is THEME-DERIVED everywhere (option a), 2026-06-16.** Dropped R `col_heatmap`'s baked blue `c("#f7fbff","#08306b")` default (→ `palette = NULL`, omitted from the wire unless explicit) AND the TS heatmap SCHEMA's matching fixed default (→ `default: null`), so both authoring paths omit the palette and the renderer's `palette ?? defaultPalette(theme)` derives the theme ramp (light surface → accent). The R↔TS default-drift is gone; the `test-parity-column-defaults.R` EXEMPT list is now EMPTY. Visible change to R heatmaps (blue → theme-accent: nejm steel-blue, terminal amber) — pre-release, acceptable, and the design vision (a default heatmap that ignored the theme contradicted theme-first). `schema-defaults.R` regenerated (palette entry dropped); R `col_heatmap` doc + the `test-column-types.R` default assertion updated. | theme-first is the vision; aligning the drift was a one-line default change on each side + a regenerate | 2026-06-16 |
| D28 | **Geometry slots are the SOURCE; the settings Geometry menu is wired for real** (maintainer caught the dead menu, 2026-06-14). The menu wrote `inputs.geometry.radius/border_width`, but the only consumers were the panel PREVIEW + the container fallback — the cell renderers HARDCODED their radii and `resolveBorders` returned FIXED per-preset rule thicknesses, ignoring the slots. Fix: (a) cell corner radii read `--tv-radius-{sm,md}` (DOM + SVG); (b) `border_preset` is now LAYOUT + a slot mapping (header→regular, group: ruled→regular else→thin, row→thin, frame→thick, gridlines→hair) with WIDTHS sourced from the geometry slots via `resolveBorders(…, resolveBorderWidths(inputs))`. `DEFAULT regular 1.5→2` (= the retired header floor). Every preset preserved byte-for-byte except frame outer 2→2.5px; it ALSO un-stuck brutalist, whose pinned sharp-corners + heavy-rules had been silently ignored. Verified: glyph-parity gate (corners) + wysiwyg gate across all 9 presets (rules). | Maintainer: "is the geometry menu wired at all? I don't see any changes." It wasn't — the controls drove a preview, not the table | 2026-06-14 |
| D26 | **Outer frame STOPS ABOVE THE AXIS** (maintainer, 2026-06-13). `border_preset=frame`/`boxed` frames the TABLE region (header top → last data row bottom), not the axis scaffold band below it. The DOM frame's existing intent was correct; the EXPORT changes to match (its rect spanned the full figure incl. axis). Implementation: a non-layout-affecting 4-side DOM frame (grid-overlay, inset box-shadow, gated on `--tv-table-border`) + the export rect clipped to the table-bottom Y + a `boxed`/`frame` visual case added to the wysiwyg matrix. Closes Bug B (docs/dev/maintainer-feedback-2026-06-13.md). | The frame reads as "the table's edge"; the axis is a separate scaffold band | 2026-06-13 |
| D9 | **REVERSED to MAXIMAL interaction defaults** (maintainer, 2026-06-13): the prior conservative split (author-grade OFF) is dropped — `BAKED_INTERACTION_DEFAULTS` now sets enableEdit / enableReorderRows / enableReorderColumns / enableAxisZoom / enableArrange ON (showGroupCounts stays off — it's a display opinion, not a tinker affordance; showFilters stays off — legacy alias). The package's primary users are developers who want every affordance available without re-enabling it per spec. Edits are LOCAL (never persist to the doc), so default-on is low-cost; a publisher targeting passive readers opts out per-spec / per-theme / via the global tier (4-tier chain unchanged). Reaches R/JS/Shiny uniformly because all consumers leave flags NA/absent and fall to the baked tier. Gates updated: interaction-resolve.test, tabviz.test; roster unchanged. REVISIT at actual ship if the audience broadens to embedded published figures (the enableThemeEdit-cog-for-readers question folds into that review). | Maintainer: "Table users are developers of the package and want to be able to check/tinker without remembering to set each time" | 2026-06-13 |
| D23 | **Polarity flip: ALL 9 PRESETS BLESS the derived flip — no pins.** 9-preset flipped battery rendered (width 380, V8 export path) and eyeballed individually: nejm/ledger/brutalist/newsprint/blueprint dark + aurora/terminal/synthwave/dwarven light are all legible and identity-coherent; the strongest are terminal-light, aurora-light, ledger-dark. One soft note: synthwave-light's bg gradient drifts olive in one corner — acceptable, revisit only if users complain. The Variations polarity control therefore shows on every preset, derive-by-default via `applyPolarityToInputs`; the pin mechanism stays available for future themes whose flip is nonsense. The battery's first run also caught a real export regression (labelWidth fallback chain missing the explicit-width rung after R's systemfonts injection) — fixed in svg-generator before the verdicts were taken. | Battery evidence; derive-by-default was already the register default and the renders confirm it | 2026-06-12 |
| D21 | **Settings UX total redesign locked** — five surfaces (Variations · Labels · Edit-theme{Identity · Plots · Styling}) per the maintainer's layer taxonomy; old panel + studio presentation SUPERSEDED (substrate unchanged); first principles + phased plan in docs/dev/settings-redesign.md. Phase 0 = removal before any new frontend element; build tab-by-tab with a consequence gate (visible-pixel delta, not DOM-fingerprint liveness). | Maintainer review found the accreted panel failing on every axis; salvage = redesign with sunk-cost gravity | 2026-06-12 |
| D22 | **Studio DORMANT** — de-documented (CTA + nav removed, dormancy notice on page); harness kept passing, not extended; reborn after core settings UX with the round-trip + validate-matrix lessons. | Maintainer: "officially dead. Will be reborn once we finish the core UX" | 2026-06-12 |
| D24 | **A11y POLISH deferred to immediately-before-CRAN** (the landed ARIA floor stays). Placeholder-title-regions idea TABLED. | Maintainer: a11y iteration needs user feedback, which needs a stable design basis | 2026-06-12 |
| D19 | **Growth requires an absorber** (maintainer-prompted, 2026-06-11): a table with NO flex/plot column and no pinned aspect sizes to its CONTENT (grid falls back to max-content; the max-content scalable hugs + centers on the shell); tables WITH a forest/viz/explicit-flex column keep filling the container. Shrink-to-fit stays universal. This re-aligns the DOM with the EXPORT's long-standing contract ("a plain table shrinks to its content" — svg-generator widthFloor), which the multi-flex arc silently broke by giving every column DEFAULT_FLEX_WEIGHT=1 against a container-width target. | Maintainer: "do tables *need* to fit the whole div? goofy for narrow tables" — and the export already answered no | 2026-06-11 |
| D18 | **Delegated-authoring principle for theme column_defaults** (maintainer ruling, 2026-06-11): authorship is DIVIDED — theme writers (institutions) are the delegated half of authoring, theme consumers (individuals) the other. Themes are afforded BROAD latitude by default; the value-equality author-wins rule ("at schema default ⇒ themeable", including the #65 re-base epsilon where an author explicitly choosing the outgoing theme's default gets re-based) is the SOUND contract, not a deficiency to engineer away with provenance marks. Provenance stays a non-goal unless real consumer complaints surface. | Maintainer ruling on propagation-readiness flag #4: institutions delegate house style to themes deliberately; an individual who fights the house style states a NON-default value, which always wins | 2026-06-11 |
| D15 | Export title-block arithmetic RESOLVED: headerBand.top max\|Δ\| 7.6 → 2.0 (budget tightened 8 → 3; the per-term metrics all read exact at ≤1). The closed-form model: exact line boxes (no per-line ceil) + title chrome (2.4 plain / 3 textured-knockout / +3 underline border) + the texture-knockout chain (2.5 replaces the themed gap on ruled/grid/dotted shells) + 1.5 under textured subtitles. Found via three instrumentation rounds (truth-to-truth metrics → ancestor var trail → computed-padding probe: the theme-runtime texture rule's `padding: 1.5px 7px` shorthand replaces the caption chains). One documented consequence: the caption fix unmasked ~0.7px of measure-loop residual on brutalist (height budget 10→11, cause annotated in the gate). Remaining +2 nejm-family residual tracked inside the tightened budget. | Closed by instrumentation — the measure-first discipline end-to-end | 2026-06-11 |
| D13 | enable_themes roster: MEASURED (the register's ask) — the resolved 9-preset roster rides interaction.enableThemes at 43.9kB ≈ 88.8% of a small widget's payload. DECIDED: switch to INPUTS-form wire envelopes resolved in-widget (~10× smaller; the widget ships the full resolver). **IMPLEMENTED same day**: serialize_theme_slim emits the roster; ThemeSwitcher expands picks via buildTheme in-widget. Measured: roster 43.9kB → 8.4kB; total widget payload 49.4kB → 14.2kB (3.5×). interaction-qa's real-input theme-switch leg green | UNILATERAL (delegated): the measurement makes keep untenable and lazy-load needless — inputs-form is strictly better on every axis | 2026-06-11 |
| D12 | Default-paginate threshold IMPLEMENTED: NULL past 200 rows auto-paginates (200 rows/page, break on group, once-per-session hint); `paginate = FALSE` is the documented opt-out; explicit specs untouched | UNILATERAL (delegated): the register default applied verbatim; pagination is the honest scale story (the DOM mount degrades before the algorithms do — bench-backed) | 2026-06-11 |
| D17 | Role-roster names KEPT as-is | UNILATERAL (delegated): the names now ship on wire 1.5–1.10 envelopes and the Stage-2/3 UI displays them daily without complaints surfacing; renaming would break existing envelopes for marginal legibility | 2026-06-11 |
| D3 | `effects.elevation` KEPT (it paints — shell-paper consumes it as figure-wide depth, decided 2026-06-05); the four orphan `--tv-shadow-raised/overlay-*` tokens + the elevation resolver group + the orphaned elevation.ts module DELETED | UNILATERAL (delegated): option (b) — the honesty rule (D2/D11 precedent); the quartet had zero consumers and TODO flags | 2026-06-11 |
| D4 | Portaled-popover neutral chrome ACCEPTED for 1.0 | UNILATERAL (delegated): default (b) — consistent neutral chrome is defensible; revisit on dark-theme adoption feedback | 2026-06-11 |
| D5 | Row-height pins stay PER-KIND only | UNILATERAL (delegated): re-confirmed — the measure loop covers per-row overflow; no new evidence since the interactivity arc | 2026-06-11 |
| D6 | Keyboard column reorder: SHIP WITHOUT, documented gap | UNILATERAL (delegated): default — expensive, rarely exercised; the a11y floor (area J) ships without it | 2026-06-11 |
| D7 | Pagination-export contract: EXPORT = WHOLE TABLE, documented | UNILATERAL (delegated): default (a) — the right semantic (a figure is the dataset, a page is a view); needs the save_plot docs sentence + WYSIWYG exception-list line when area G writes them | 2026-06-11 |
| D8 | Estimator-vs-canvas widths on raw generateSVG: **RESOLVED** (2026-06-13) — both halves closed, not merely budgeted. MEASUREMENT: the hand-tuned character-class multipliers (a magic-number accretion that over-budgeted ~7%) were replaced by REAL per-glyph advance tables measured offline from every preset's actual webfonts at anchor weights 400/700 (`font-metrics.generated.ts`, regen via `npm run regen:font-metrics`), with serif/sans/mono class fallbacks for unknown families and per-glyph weight interpolation across the continuous axis — residual vs canvas is kerning only (~1–2%). FLEX-PARITY: the estimator fix unmasked a real divergence (the label column flexed in the DOM but was a separate scalar in the export, and viz naturals mismatched), root-caused to D20 item-4 and fixed there. The WYSIWYG gate now passes the raw-generateSVG path at **0 breaches** (155 findings, down from 178). | the over-budget was a bug, not a boundary; empirical measurement + flex unification dissolved it | 2026-06-13 |
| D10 | Group-header banding/chevron: VERIFIED ALIGNED by measurement (2026-06-11) — a new permanent matrix metric (groupLabel.x, DOM post-chevron vs SVG text x) agrees within 1.5px across all 12 cases; the export's banding coordination already exists (renderGroupHeader's renderBackground handshake). The 2026-06-10 premise was closed by the intervening WYSIWYG pass + W4 ports. The probe guards regressions from here. | UNILATERAL (delegated): measurement preempted implementation | 2026-06-11 |
| D16 | Settings panel single-scroll RESTORED + IMPLEMENTED: compact layout renders all four sections in one scroll with the bar as sticky jump-links (role=navigation); the studio (roomy) keeps real tabs; the liveness harness rewritten for the new IA — 70 controls verified live (up from 68), reset gutter green | UNILATERAL (delegated): default (a) — the settings-overhaul lock had reviewed rationale | 2026-06-11 |
| D18 | **REVERSED 2026-06-15** — title color now defaults to role `brand-text` (the brand scale), not neutral `text`. ~~Title color bound to role `text` (option a)~~. The maintainer, reviewing live output, found the neutral title read as unbranded ("I thought the title was using the brand scale"); a chromatic title IS the figure's identity, and "re-identify all 9 presets" is now the DESIRED outcome, not a cost. `brand-text` is contrast-validated so it stays readable on every preset (verified: nejm crimson on warm paper, terminal green on dark). The original flag was "chromatic titles stay one re-route away" — flipped: NEUTRAL titles are now the one re-route away (`set_component("title", col = "text")`). 27 cssVars snapshots regenerated (only `--tv-text-title-fg` changed). | 2026-06-15 |
| D11 | consumedBy annotation arc EXECUTED as one dedicated arc: all 119 grandfathered rows cleared (annotated against verified consumers, or deleted as dead — found currency:symbol/position dead editor menus + viz_violin:maxWidth) | The sweep itself caught 3 more dead options — exactly the audit dividend the one-arc approach promised | 2026-06-11 |
| D2 | The 6 authored-but-ignored column options DELETED (`range:showBar`, `viz_bar:{barWidth,barGap,orientation}`, `viz_boxplot:{boxWidth,whiskerType}`) — schema rows, typed fields, authoring args (TS + R), emit defaults, docs, drift-gate grandfather rows | Register default applied at M1: unread knobs violate the honesty rule; renderers compute these locally (VizBar hardcodes its own gap) and never read the options; plausible features can return post-1.0 WITH readers | 2026-06-11 |
| D1 | HC-fidelity vertical KILLED (rules + `--tv-hc-*` tokens + resolver group + test deleted) | Designed but never wired to any markup; the a11y floor (roadmap J) lands without it — HC mode's live behavior (wash drops, border bump, contrast re-resolve) is untouched | 2026-06-10 |
| D14 | `theme_blend()` + SplitForest static-knit path CUT from 1.0 scope | Neither is started; neither blocks the 1.0 identity; post-1.0 backlog | 2026-06-10 |

## Process notes

- The register is for PRODUCT decisions (behavior, scope, defaults), not
  engineering choices an arc can settle locally.
- "Default if undecided" is binding: if the decide-by milestone closes
  without a decision, the default ships. This is what makes deferral safe.
- Sources feeding the initial population: interactivity-UX arc deferrals,
  WYSIWYG fidelity pass residuals, 2026-06-10 cleanup audit "kept for
  decision" items, 1.0 strategy locks.
