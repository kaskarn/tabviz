% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shiny.R
\name{tabviz_state}
\alias{tabviz_state}
\title{Read the full observability state of a tabviz widget}
\usage{
tabviz_state(input, id)
}
\arguments{
\item{input}{The Shiny \code{input} object}

\item{id}{The widget's output id (the same one passed to \code{tabvizOutput()})}
}
\value{
A named list with one entry per dimension. Missing inputs (the
widget hasn't emitted yet) come through as \code{NULL}.
}
\description{
Pulls every per-dimension Shiny input the widget emits into a single named
list. The envelope (\code{source}, \code{ts}) is stripped — the returned list is the
raw values, suitable for direct use in dashboard logic. Use
\code{\link[=tabviz_state_envelope]{tabviz_state_envelope()}} when you need to filter on the provenance tag.
}
\details{
Two-way Shiny integration:
\itemize{
\item \strong{Widget → Shiny}: clicking a column header emits \verb{input$<id>_sort}.
\item \strong{Shiny → Widget}: dashboards push state via \code{\link[=tabviz_proxy]{tabviz_proxy()}} and the
fluent verbs (\code{\link[=sort_rows]{sort_rows()}}, \code{\link[=filter_rows]{filter_rows()}}, ...).
}

Dashboards that mirror widget state will typically also want to filter
out their own writes. That's what \code{\link[=tabviz_state_envelope]{tabviz_state_envelope()}} is for —
check \verb{state$<dim>$source == "user"} to react only to user-driven changes.
}
\seealso{
\code{\link[=tabviz_state_envelope]{tabviz_state_envelope()}}, \code{\link[=tabviz_proxy]{tabviz_proxy()}}
}
