% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/themes-api.R
\name{web_theme}
\alias{web_theme}
\title{Build a theme from V4 anchors.}
\usage{
web_theme(
  paper = NULL,
  ink = NULL,
  brand = DEFAULT_BRAND_HEX,
  accent = NULL,
  polarity = "light",
  monochrome = FALSE,
  mode = "standard",
  categorical = "okabe_ito",
  sequential = "viridis",
  diverging = "rdbu",
  status_positive = NULL,
  status_negative = NULL,
  status_warning = NULL,
  status_info = NULL,
  fonts_body = NULL,
  fonts_display = NULL,
  fonts_mono = NULL,
  fonts_numeric = NULL,
  density = "comfortable",
  density_factor = 1,
  shell_mode = NULL,
  shell_texture = NULL,
  type_base_size = NULL,
  type_scale_ratio = NULL,
  type_weights = NULL,
  curves = NULL,
  geometry = NULL,
  effects = NULL,
  marks = NULL,
  row_kinds = NULL,
  header_style = NULL,
  border_preset = NULL,
  first_column_style = "default",
  web_fonts = NULL,
  name = "custom"
)
}
\arguments{
\item{paper}{Light-end neutral anchor (hex or \code{\link[=oklch]{oklch()}} triple). Defines
surface, paper_alt, paper_raised. Default: near-white at brand hue.}

\item{ink}{Dark-end neutral anchor. Defines text, text-muted, text-subtle.}

\item{brand}{Identity hue. Drives brand_solid, brand_text, header_bg.}

\item{accent}{Optional engagement hue (hover/selected/callouts). NULL
mirrors brand at resolution.}

\item{polarity}{\code{"light"} or \code{"dark"}. Polarity reflection inverts every
anchor's L around the midpoint. Default \code{"light"}.}

\item{monochrome}{When \code{TRUE} the neutral ramp rides the brand hue with
chroma shaped from brand C (B8) — phosphor / sepia / cyanotype
single-hue themes in one toggle. Default \code{FALSE}.}

\item{mode}{Accessibility axis (orthogonal to polarity). One of
\code{"standard"} / \code{"high-contrast"} / \code{"reduced-transparency"}. HC bumps
border-widths +1px and drops every Phase D effect; RT keeps glow but
swaps gradients to a flat surface fill. Default \code{"standard"}.}

\item{categorical}{Named data scheme reference (Okabe-Ito default).}

\item{sequential}{Named sequential scheme reference.}

\item{diverging}{Named diverging scheme reference.}

\item{status_positive, status_negative, status_warning, status_info}{Optional status anchor overrides (hex or \code{\link[=oklch]{oklch()}}). NULL defers to
the TS resolver's defaults.}

\item{fonts_body, fonts_display, fonts_mono}{Font stacks. fonts_display NULL
mirrors fonts_body.}

\item{fonts_numeric}{Dedicated figure font for number columns. NULL falls
back to fonts_body (numbers follow the body font, never the mono).}

\item{density}{\code{"compact"}, \code{"comfortable"}, or \code{"spacious"}.}

\item{density_factor}{Continuous multiplier on the density preset's spacing,
in \verb{[0.5, 2]} (1 = preset unchanged) — a fine dial on top of the named
profile (e.g. \code{0.9} for a touch tighter).}

\item{shell_mode}{Stage 2 shell/paper two-surface model. One of
\code{"flush"} / \code{"raised"} / \code{"float"} / \code{"transparent"}. NULL defaults to
\code{"flush"} at resolution.}

\item{shell_texture}{Stage 2 surface texture. One of \code{"none"} /
\code{"ruled"} / \code{"grid"} / \code{"dotted"} / \code{"grain"}. NULL defaults to \code{"none"}.}

\item{type_base_size}{Stage 2 typography Tier 1 — anchor of the modular
size scale (px). NULL defaults to 14.}

\item{type_scale_ratio}{Stage 2 typography Tier 1 — exponential ratio
between scale steps. NULL defaults to 1.2.}

\item{type_weights}{Stage 2 typography Tier 1 — named numeric list with
any of \code{regular}, \code{medium}, \code{semibold}, \code{bold}. NULL defaults to
400/500/600/700.}

\item{curves}{Stage 1 §25 per-ramp curve shape — named list with any of
\code{neutral}, \code{brand}, \code{accent} keyed to \code{"linear"} / \code{"ease"} / \code{"smooth"}
/ \code{"log"} / \code{"exp"}. NULL defaults: neutral=ease, brand=linear,
accent=linear.}

\item{geometry}{Phase D GEOMETRY axis — named list with optional \code{radius}
(named list of \code{sm}/\code{md}/\code{lg}/\code{pill} numeric px) and \code{border_width}
(named list of \code{hair}/\code{thin}/\code{regular}/\code{thick} numeric px). Drives
corner softness + line weight across the widget. NULL = TS defaults
(2/6/10/999 px radius, 0.5/1/1.5/2.5 px border-width).}

\item{effects}{Phase D EFFECTS axis — named list with optional
\code{glow_intensity} (\code{"none"} / \code{"subtle"} / \code{"neon"}), \code{glow_anchor}
(\code{"brand"} / \code{"accent"}), \code{gradient_shell_intensity} (\code{"none"} /
\code{"subtle"} / \code{"vivid"}), \code{gradient_shell_angle} (degrees 0-360),
\code{elevation} (\code{"none"} / \code{"low"} / \code{"medium"} / \code{"high"}). NULL =
no effects (the safe editorial baseline). HC mode drops all effects.}

\item{marks}{Viz-mark identity (D12): named list with \code{point_shape}
(\code{"circle"}/\code{"square"}/\code{"diamond"}/\code{"triangle"}) and/or
\code{interval_weight} (\code{"hair"}/\code{"regular"}/\code{"thick"}). Theme-level
defaults; per-row and per-effect styling still win.}

\item{row_kinds}{Per-row-kind theme-default \code{heightRatio} map. Named
list whose keys are row-kind names (\code{data}, \code{group_header}, \code{spacer},
\code{summary}, \code{header}, \code{panel}) and whose values are themselves
named lists with \code{heightRatio} (numeric, multiplies the base row
height). Layer 3 of the row-kind height cascade (Stage 1 §33);
constructor \verb{row_heights=} and user pins layer above this. NULL =
row-kind intrinsics apply.}

\item{header_style}{Header chrome treatment (a structural variant
input): \code{"light"}, \code{"tint"}, or
\code{"bold"}. Default \code{"light"}.}

\item{border_preset}{Border treatment (a Tier-1 structural enum): one of
\code{"none"}, \code{"hairline"}, \code{"ruled"}, \code{"frame"}, or \code{"boxed"}. \code{"frame"} is
the clean journal look (top+bottom table frame). Default resolver cluster
(≈ hairline) when unset.}

\item{first_column_style}{First (label) column treatment: \code{"default"},
\code{"tint"}, or \code{"bold"}. Default \code{"default"}.}

\item{web_fonts}{Optional list of \code{\link[=web_font]{web_font()}} declarations to embed.}

\item{name}{Theme name (string).}
}
\value{
A fully-resolved \link{WebTheme}.
}
\description{
The user-authoring surface. V4 vocabulary: identity is four named OKLCH
anchors — \code{paper}, \code{ink}, \code{brand}, optional \code{accent}. Each accepts a
hex string OR an \code{\link[=oklch]{oklch()}} triple. Polarity reflection acts on each
anchor's L. \code{accent} defaults to \code{brand} when unset. Status anchors
(\verb{status_*}) take the same hex-or-\code{\link[=oklch]{oklch()}} form.
}
\examples{
# The usual on-ramp: start from a preset, swap the brand (C56).
th <- set_brand(web_theme_nejm(), "#006266")

# Rubrication (significance stars, caption chips) follows `accent` by
# default; pin --tv-ink2 to make it differ from the interaction hue:
th <- set_pin(web_theme_nejm(), "--tv-ink2", "#D42320")

# Monochrome (B8): one toggle for phosphor/sepia/cyanotype themes.
th <- web_theme(brand = "#20C45F", polarity = "dark", monochrome = TRUE)

# Atomic status palette (D4):
th <- set_status_palette(th, list(negative = "#D42320"))
}
