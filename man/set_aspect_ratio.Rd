% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modifiers.R
\name{set_aspect_ratio}
\alias{set_aspect_ratio}
\title{Pin a target aspect ratio on a tabviz}
\usage{
set_aspect_ratio(x, ratio)
}
\arguments{
\item{x}{A \code{WebSpec} object, htmlwidget, or \code{tabviz_proxy}.}

\item{ratio}{Numeric target aspect ratio (\code{width / height}). Pass
\code{NULL} to clear (render at natural).}
}
\value{
The modified WebSpec, htmlwidget, or proxy (invisibly).
}
\description{
Sets the spec's \code{target_aspect} field (read by \code{save_plot()} as the
default for \code{ratio}, and by the widget's interactive control as the
starting position). \code{set_aspect_ratio(NULL)} clears the target —
equivalent to "render at natural aspect".
}
\details{
Three input surfaces share the same machinery:

\describe{
\item{Fluent (WebSpec / htmlwidget)}{Pins the target on the spec.
The widget renders at that aspect; \code{save_plot()} honours it.}
\item{\code{save_plot(..., ratio = X)}}{Save-time override of the spec's
target for that render only.}
\item{Proxy (\code{tabviz_proxy})}{Sends a \code{setAspectRatio} message to
the running widget — same path as \code{set_theme()} etc.}
}
}
\examples{
\dontrun{
tabviz(data, label = "study", columns = list(viz_forest("hr", "lo", "hi"))) |>
  set_aspect_ratio(2)            # 2:1 wide
}

}
