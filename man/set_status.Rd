% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/themes-api.R
\name{set_status}
\alias{set_status}
\title{Set status anchor overrides (positive / negative / warning / info) and re-resolve.}
\usage{
set_status(
  theme,
  positive = NULL,
  negative = NULL,
  warning = NULL,
  info = NULL
)
}
\arguments{
\item{theme}{A \link{WebTheme}.}

\item{positive, negative, warning, info}{Hex / \code{\link[=oklch]{oklch()}} / \code{NA} to clear
/ \code{NULL} to leave unchanged.}
}
\value{
The re-resolved \link{WebTheme}.
}
\description{
Set status anchor overrides (positive / negative / warning / info) and re-resolve.
}
\seealso{
\code{\link[=set_status_palette]{set_status_palette()}} for an atomic whole-palette clobber.

Each arg accepts a hex string, an \code{\link[=oklch]{oklch()}} triple, \code{NA} (clear the
override; TS resolver falls back to its curated default), or \code{NULL}
(leave the slot unchanged — the default — so partial updates touch
only the args you pass).
}
