% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/themes-api.R
\name{set_component}
\alias{set_component}
\title{Re-route a component channel to a different role (the middle verb)}
\usage{
set_component(theme, component, ..., state = "base")
}
\arguments{
\item{theme}{A \link{WebTheme}.}

\item{component}{Component name — see \code{\link[=list_components]{list_components()}}.}

\item{...}{Named channel values, e.g. \code{col = "accent-text"},
\code{family = "mono"}, \code{weight = "semibold"}.}

\item{state}{Component state the bindings apply to (default \code{"base"}).}
}
\value{
The \link{WebTheme} re-resolved with the channels re-routed.
}
\description{
The component model's editing grammar has three verbs
(\code{docs/dev/component-model.md}):
\enumerate{
\item \strong{Re-tune a role} — \code{\link[=set_role]{set_role()}} / \code{\link[=set_type_role]{set_type_role()}}:
global; everything bound to the role follows.
\item \strong{Re-route a component channel} — \code{set_component()}:
local to one component, but cascade-coherent — the re-routed role
still re-resolves under polarity flips, anchor edits, and
high-contrast mode (unlike a raw pin).
\item \strong{Pin} — \code{\link[=set_pin]{set_pin()}}: token to raw value; last resort.
}
}
\details{
Components are curated, named table parts organized by region (see
\code{\link[=list_components]{list_components()}}): \code{"title"}, \code{"row"}, \code{"header-cell"},
\code{"axis-label"}, …. Each exposes typed channels: color channels
(\code{col}, \code{bg}, \code{bar}, \code{rule}) take a Tier-2 color role name; type
channels (\code{family}, \code{size}, \code{weight}) take the typography slot
vocabulary. States are sparse (\code{base} implied; rows add
\code{alt}/\code{hover}/\code{selected}/\code{emphasis}; the header's three style variants
are states \code{light}/\code{tint}/\code{fill}).
}
\examples{
\dontrun{
web_theme_nejm() |>
  set_component("title", col = "accent-text") |>
  set_component("cell", family = "mono") |>
  set_component("row", state = "emphasis", bar = "accent-solid")
}
}
\seealso{
\code{\link[=clear_component]{clear_component()}}, \code{\link[=list_components]{list_components()}}, \code{\link[=set_role]{set_role()}},
\code{\link[=set_pin]{set_pin()}}
}
