% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-nse.R
\name{resolve_style_exprs}
\alias{resolve_style_exprs}
\title{Resolve multiple style expressions}
\usage{
resolve_style_exprs(data, ..., .env = parent.frame())
}
\arguments{
\item{data}{Data frame}

\item{...}{Named style expressions (e.g., row_bold = ~ p < 0.05)}

\item{.env}{Evaluation environment}
}
\value{
List with:
\itemize{
\item \code{data}: Modified data frame
\item Named elements for each resolved column name
}
}
\description{
Convenience function to resolve multiple row styling parameters at once.
Modifies the data frame in place and returns column names.
}
\examples{
\dontrun{
df <- data.frame(p = c(0.01, 0.1), is_key = c(TRUE, FALSE))

result <- resolve_style_exprs(
  df,
  row_bold = ~ p < 0.05,
  row_emphasis = "is_key"
)
# result$data has new computed column
# result$row_bold is the column name
# result$row_emphasis is "is_key"
}
}
\keyword{internal}
