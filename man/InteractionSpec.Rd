% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classes-components.R
\name{InteractionSpec}
\alias{InteractionSpec}
\title{InteractionSpec: Interaction settings}
\usage{
InteractionSpec(
  show_filters = FALSE,
  show_legend = TRUE,
  enable_sort = TRUE,
  enable_collapse = TRUE,
  enable_select = TRUE,
  enable_hover = TRUE,
  enable_resize = TRUE,
  enable_export = TRUE,
  enable_filters = TRUE,
  enable_reorder_rows = TRUE,
  enable_reorder_columns = TRUE,
  enable_edit = TRUE,
  show_group_counts = FALSE,
  tooltip_fields = NULL,
  enable_themes = "default"
)
}
\arguments{
\item{show_filters}{(Deprecated) Alias for \code{enable_filters}}

\item{show_legend}{Show legend}

\item{enable_sort}{Enable column sorting}

\item{enable_collapse}{Enable group collapsing}

\item{enable_select}{Enable row selection}

\item{enable_hover}{Enable hover effects}

\item{enable_resize}{Enable column resizing}

\item{enable_export}{Enable download/export button}

\item{enable_filters}{Enable per-column filter popovers in column headers}

\item{enable_reorder_rows}{Enable drag-and-drop reordering of rows (within a group)
and row-groups (among siblings sharing a parent). Session-only; WYSIWYG export
reflects the reorder automatically.}

\item{enable_reorder_columns}{Enable drag-and-drop reordering of columns (within a
column group) and column-groups (among top-level siblings).}

\item{enable_edit}{Enable double-click inline editing for text/numeric/label cells,
and a popover editor for forest-cell numerics (estimate / lower / upper).}

\item{show_group_counts}{Show the row count in parentheses next to each row-group
header label (e.g. "Main Trials (12)"). Default \code{FALSE}.}

\item{tooltip_fields}{Character vector of column names to show in hover tooltip (NULL = no tooltip)}

\item{enable_themes}{Control the interactive theme-switcher menu. Accepts:
\itemize{
\item \code{"default"}: show all \code{\link[=package_themes]{package_themes()}}.
\item \code{NULL}: hide the theme switcher entirely.
\item A flat list of \code{WebTheme} objects: show exactly those themes (no tabs).
Named list entries override each theme's display name
(e.g. \code{list(Classical = web_theme_jama())}).
\item A 2-level named list of named lists of \code{WebTheme}: show those themes
grouped under category tabs. The keys of the outer list become tab
labels (e.g. \code{list(Editorial = list(Cochrane = web_theme_cochrane(), Lancet = web_theme_lancet()), Other = list(Dark = web_theme_dark()))}).
In all list forms the spec's active \code{theme} is auto-included so users
can always revert.
}

Defaults to \code{getOption("tabviz.enable_themes", "default")}, so a
session-wide curated list can be set once via
\code{options(tabviz.enable_themes = list(...))}. See also
\code{\link[=selectable_themes]{selectable_themes()}} for a fluent modifier.}
}
\description{
InteractionSpec: Interaction settings
}
