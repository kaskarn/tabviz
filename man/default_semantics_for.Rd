% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classes-theme.R
\name{default_semantics_for}
\alias{default_semantics_for}
\title{Build per-token default bundles from a ColorPalette + Typography}
\usage{
default_semantics_for(colors, typography)
}
\arguments{
\item{colors}{A \link{ColorPalette} object.}

\item{typography}{A \link{Typography} object.}
}
\value{
A \link{Semantics} object whose bundles mirror the old hardcoded
rendering semantics against the supplied palette / typography.
}
\description{
Encodes the historical rendering rules -- \code{emphasis} = foreground + bold,
\code{muted} = muted color, \code{accent} = accent color (both for text and marker
fill). Called by every \verb{web_theme_*()} preset helper so bundle defaults
track each theme's palette without per-theme duplication.
}
\keyword{internal}
