% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classes-components.R
\name{col_pictogram}
\alias{col_pictogram}
\title{Column helper: Pictogram}
\usage{
col_pictogram(
  field,
  header = NULL,
  width = NULL,
  glyph = "person",
  glyph_field = NULL,
  max_glyphs = NULL,
  half_glyphs = FALSE,
  min_value = NULL,
  max_value = NULL,
  color = NULL,
  empty_color = NULL,
  size = c("base", "sm", "lg"),
  layout = c("row", "stack"),
  value_label = FALSE,
  label_format = NULL,
  label_decimals = 1,
  na_text = NULL,
  ...
)
}
\arguments{
\item{field}{Field name containing the numeric value driving the count.}

\item{header}{Column header (default NULL, uses field name).}

\item{width}{Column width in pixels (NULL for auto-sizing).}

\item{glyph}{Either a single string (registry name like \code{"person"},
\code{"skull"}, etc., or a literal unicode character like \code{"★"}) OR a
named character vector mapping row-field values to glyph specs (e.g.
\code{c("low" = "leaf", "high" = "flame")}). See \code{\link[=glyph_registry_names]{glyph_registry_names()}}
for the bundled set.}

\item{glyph_field}{When \code{glyph} is a named vector, the field whose row
values index into it. Required for the mapping form.}

\item{max_glyphs}{Numeric scalar. \code{NULL} (default) → count mode (render
\code{round(value)} glyphs). Integer → rating mode (render exactly
\code{max_glyphs} glyphs, first \code{value} filled).}

\item{half_glyphs}{Allow half-fill on the boundary glyph (default
\code{FALSE}).}

\item{min_value, max_value}{Numeric scalars defining the input range to
remap into \verb{[0, max_glyphs]} (rating mode only). For example,
\verb{min_value = 0, max_value = 100} with \code{max_glyphs = 5} maps a value
of 75 to 3.75 glyphs. Pass both or neither.}

\item{color}{Filled glyph color. \code{NULL} (default) → \code{var(--tv-accent)}
so the column follows the active theme.}

\item{empty_color}{Empty (ghost) glyph color, rating mode only. \code{NULL}
(default) → \code{var(--tv-muted)}.}

\item{size}{Glyph size: \code{"sm"}, \code{"base"} (default), or \code{"lg"}. Matches
the convention used by \code{\link[=col_stars]{col_stars()}}, \code{\link[=col_icon]{col_icon()}}, \code{\link[=col_badge]{col_badge()}}.}

\item{layout}{\code{"row"} (default) or \code{"stack"} (vertical pile). Stack
mode renders best in tall rows (e.g. rows containing a \verb{viz_*}
column); in normal rows it may overflow.}

\item{value_label}{\code{FALSE} (default), \code{TRUE} (= \code{"trailing"}),
\code{"leading"}, or \code{"trailing"}. When set, render the numeric value
alongside the glyphs.}

\item{label_format}{\code{NULL} (default = \code{"decimal"}), \code{"integer"}, or
\code{"decimal"}. Controls how the value label is formatted.}

\item{label_decimals}{Number of decimals when \code{label_format = "decimal"}
(default 1).}

\item{na_text}{Text to display for NA/missing values (default NULL = blank).}

\item{...}{Additional arguments passed to \code{web_col()}, including cell
styling. \code{style_muted} dims the glyphs (opacity 0.5); \code{style_color}
overrides the filled glyph color.}
}
\value{
A ColumnSpec object with type = "pictogram".
}
\description{
Repeat a glyph proportional to a numeric value. Two modes are supported:
}
\details{
\itemize{
\item \strong{Count mode} (default; \code{max_glyphs = NULL}): render \code{round(value)}
copies of \code{glyph}. No empty slots. Suitable for "show 9 stick figures
for a score of 9" patterns.
\item \strong{Rating mode} (\code{max_glyphs = N}): render \code{N} glyphs total, with the
first \code{value} filled and the remaining shown as empty (ghost) glyphs
in \code{empty_color}. Suitable for "3 of 5 skulls" displays.
}

Glyphs may be either a registry name (see \code{\link[=glyph_registry_names]{glyph_registry_names()}}) or
a literal unicode character. A named character vector keyed to a row
field selects the glyph per row (mirroring \code{\link[=col_icon]{col_icon()}}'s \code{mapping}).

Theme-aware defaults: when \code{color} / \code{empty_color} are \code{NULL}, the
rendered glyphs read from \code{--tv-accent} and \code{--tv-muted} so they follow
the active theme.
}
\examples{
# Count mode: stick figures with trailing label
col_pictogram("score", glyph = "person", value_label = TRUE,
              label_format = "decimal", label_decimals = 1)

# Rating mode: 3 of 5 skulls
col_pictogram("perf", glyph = "skull", max_glyphs = 5,
              color = "#f97316", empty_color = "#3f3f46")

# Per-row glyph selection
col_pictogram("count", glyph = c("low" = "leaf", "high" = "flame"),
              glyph_field = "category")

# Vertical stack with numeric label (use in tall rows)
col_pictogram("eaten", glyph = "coin", layout = "stack",
              value_label = "trailing")
}
