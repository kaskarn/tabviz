% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classes-components.R
\name{col_badge}
\alias{col_badge}
\title{Column helper: Status badges}
\usage{
col_badge(
  field,
  header = NULL,
  width = NULL,
  variants = NULL,
  colors = NULL,
  size = c("base", "sm"),
  shape = c("pill", "circle", "square"),
  outline = FALSE,
  thresholds = NULL,
  na_text = NULL,
  ...
)
}
\arguments{
\item{field}{Field name containing the badge text}

\item{header}{Column header (default NULL, uses field name)}

\item{width}{Column width in pixels (NULL for auto-sizing based on content)}

\item{variants}{Named character vector mapping values to semantic variants:
"default", "success", "warning", "error", "info", "muted"
(e.g., \code{c("published" = "success", "draft" = "warning")})}

\item{colors}{Named character vector mapping values to custom hex colors,
which override variants (e.g., \code{c("special" = "#ff5500")}). When
\code{thresholds} is supplied, \code{colors} must instead be an unnamed character
vector with \code{length(thresholds) + 1} entries.}

\item{size}{Badge size: "sm" or "base" (default "base")}

\item{shape}{Badge shape: \code{"pill"} (default), \code{"circle"}, or \code{"square"}.}

\item{outline}{Logical; if \code{TRUE}, render outline-only badges (transparent
fill with a colored border).}

\item{thresholds}{Numeric vector of sorted breakpoints for binning numeric
values into colored badges. Mutually exclusive with \code{variants}. Pair with
an unnamed \code{colors} vector of length \code{length(thresholds) + 1}.}

\item{na_text}{Text to display for NA/missing values (default NULL = blank)}

\item{...}{Additional arguments passed to \code{web_col()}, including cell styling
(\code{bold}, \code{italic}, \code{color}, \code{bg}, \code{emphasis}, \code{muted}, \code{accent}) and
alignment. Body cells default to \code{align = "center"} (override with
\code{align = "left"} / \code{"right"}); header alignment follows the body unless
\code{header_align} is set explicitly.}
}
\value{
A ColumnSpec object
}
\description{
Display colored status badges (pills) based on data values.
}
\examples{
# Simple badge (shows value as badge text)
col_badge("status")

# With semantic variants
col_badge("status", variants = c(
  "published" = "success",
  "draft" = "warning",
  "rejected" = "error"
))

# With custom colors
col_badge("priority", colors = c(
  "high" = "#dc2626",
  "medium" = "#f59e0b",
  "low" = "#22c55e"
))

# With per-cell styling
col_badge("status", emphasis = "is_key")
}
