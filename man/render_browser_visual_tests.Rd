% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render_browser_visual_tests.R
\name{render_browser_visual_tests}
\alias{render_browser_visual_tests}
\title{Browser visual-regression harness}
\usage{
render_browser_visual_tests(pattern = NULL, update = FALSE, verbose = TRUE)
}
\arguments{
\item{pattern}{Optional regex; when supplied, restricts the run to
gallery example names matching it.}

\item{update}{Logical. When \code{TRUE}, baselines are overwritten with
the current rendered output. Use this after an intentional
rendering change, with a commit message explaining what changed.}

\item{verbose}{Logical; print per-spec status as snapshots are taken.}
}
\value{
Invisibly, the integer exit code from the snapshot script.
\code{0} = all snapshots match. \code{1} = at least one diff or new fixture.
\code{2} = harness error.
}
\description{
Renders a small set of canonical example specs to self-contained
HTML files, then hands them off to the Puppeteer-based snapshot
script (\code{srcjs/tests/visual-browser/snapshot.mjs}) which produces
PNG screenshots and diffs against baseline images.
}
\details{
Complements \code{\link[=render_visual_tests]{render_visual_tests()}} which exercises the V8/SVG
export path. The two harnesses cover different runtime paths
(DOM-mounted Svelte vs. serialized SVG), and a regression in one
can pass undetected by the other. See
\code{srcjs/tests/visual-browser/README.md} for harness details.
}
\examples{
\dontrun{
# Run all browser visual tests
tabviz::render_browser_visual_tests()

# Just the gallery jamA examples
tabviz::render_browser_visual_tests("jama")

# Accept the current output as the new baseline
tabviz::render_browser_visual_tests(update = TRUE)
}
}
