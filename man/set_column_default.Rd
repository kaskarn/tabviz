% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/themes-api.R
\name{set_column_default}
\alias{set_column_default}
\title{Set a theme's house-style default options for a column type}
\usage{
set_column_default(theme, type, ...)
}
\arguments{
\item{theme}{A \link{WebTheme}.}

\item{type}{A column type string, e.g. \code{"pvalue"}, \code{"numeric"}, \code{"bar"}.}

\item{...}{Named option values to default for that type, e.g.
\verb{stars = TRUE, significantStyle = "pill"}. Passing no options clears any
existing defaults for \code{type}.}
}
\value{
The \link{WebTheme} re-resolved with the column defaults recorded.
}
\description{
Theme-as-house-style: declare per-column-TYPE default options that merge
UNDER every matching column at render time. For example, a clinical theme
can make all p-value columns show significance stars and a pill by default,
without the table author touching each column.
}
\details{
Two rules keep this safe, both enforced by the engine at spec-ingest:
\itemize{
\item \strong{The author always wins.} A theme default fills only an
option the column left at its own default; an explicit
\code{col_pvalue(stars = FALSE)} is never overridden.
\item \strong{Presentation only.} Only \code{styling}/\code{editor} options are
accepted. Options that change what the data \emph{means} (precision,
thresholds, the bound field) are dropped — a theme can restyle, never
re-compute.
}
}
\examples{
\dontrun{
web_theme_nejm() |>
  set_column_default("pvalue", stars = TRUE, significantStyle = "pill")
}
}
\seealso{
\code{\link[=web_theme]{web_theme()}}, \code{\link[=set_type_role]{set_type_role()}}
}
