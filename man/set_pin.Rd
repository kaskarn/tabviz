% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/themes-api.R
\name{set_pin}
\alias{set_pin}
\title{Pin a component token to a direct value.}
\usage{
set_pin(theme, css_var, value)
}
\arguments{
\item{theme}{A \link{WebTheme}.}

\item{css_var}{Manifest token name (e.g. \code{"--tv-text-footnote-size"};
the \verb{--tv-} prefix may be omitted).}

\item{value}{CSS value string (e.g. \code{"0.7rem"}, \code{"#1a2b3c"}).}
}
\value{
The \link{WebTheme} re-resolved with the token pinned.
}
\description{
The R twin of the studio's token-pin channel (settings-overhaul P3):
writes a manifest cssVar directly, overriding whatever the cascade
derives. Total control — the value is applied after resolution and
before contrast validation, and rides the portable theme artifact
(the wire's \code{pins}). Use \code{\link[=list_component_tokens]{list_component_tokens()}} to discover names.
}
\examples{
\dontrun{
web_theme_nejm() |>
  set_pin("--tv-text-footnote-size", "0.7rem") |>
  set_pin("text-title-weight", "700")
}
}
\seealso{
\code{\link[=clear_pin]{clear_pin()}}, \code{\link[=set_role]{set_role()}}
}
