% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/debug_shapes.R
\name{render_debug_shapes}
\alias{render_debug_shapes}
\title{Render the box-model debug view ("debug-shapes")}
\usage{
render_debug_shapes(spec, file = NULL, width = NULL)
}
\arguments{
\item{spec}{A \code{WebSpec} (e.g. from \code{\link[=forest_plot]{forest_plot()}} or \code{\link[=tabviz]{tabviz()}}).}

\item{file}{Optional output path. If it ends in \code{.png} and the \code{rsvg}
package is available, a PNG is written; otherwise an \code{.svg} is written.
When \code{NULL}, the raw SVG string is returned invisibly.}

\item{width}{PNG raster width in px (only when writing a PNG). \code{NULL} uses
the SVG's natural width.}
}
\value{
The SVG string (invisibly when \code{file} is written).
}
\description{
The \emph{visual} half of the sizing-verification harness (the numeric half is
the TS \code{computeLayoutMetrics} snapshot gate). Renders a spec's box model as
an SVG of labeled boxes instead of real cell content: every cell shows its
allocated box, horizontal padding regions (pink), the text-anchor inset
(red dot), the row marker-center (blue dash), and the row kind (fill color).
}
\details{
Strips content so sizing errors become eyeball-obvious. Because it is a pure
function of the same \code{computeLayoutMetrics} the numeric gate snapshots, the
visual and numeric views agree by construction. Use it to sanity-check
geometry while working on the sizing / row-kind subsystems.
}
\seealso{
\code{docs/dev/sizing-model.md} (§6b) for the harness design.
}
