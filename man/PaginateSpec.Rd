% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classes-core.R
\name{PaginateSpec}
\alias{PaginateSpec}
\title{PaginateSpec: Pagination configuration for a tabviz}
\usage{
PaginateSpec(
  rows = 30L,
  break_on = "split",
  keep_groups = TRUE,
  orphan_min = 3L,
  repeat_header = TRUE,
  repeat_legend = TRUE,
  repeat_title = TRUE,
  footnotes_on = "last",
  page_label = "x_of_y",
  oversized_group_policy = "overflow"
)
}
\description{
Use \code{\link[=paginate_spec]{paginate_spec()}} (or shortcuts \code{paginate = TRUE} / \verb{paginate = <int>}
/ \code{paginate_letter()} / \code{paginate_a4()} / \code{paginate_slide()}) on \code{tabviz()}
to break long tables into multiple pages. The HTML viewer renders one page
at a time with prev/next controls; PDF export emits one logical page per
PDF page (merged via \code{qpdf::pdf_combine}).
}
\details{
Breakpoints are computed once on the R side and stored on the wire so the
HTML viewer and PDF export agree on where pages start and end.
}
