% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modifiers.R
\name{paginate}
\alias{paginate}
\title{Configure pagination on an existing WebSpec or htmlwidget}
\usage{
paginate(x, ...)
}
\arguments{
\item{x}{A \code{WebSpec} object or an htmlwidget created by \code{\link[=tabviz]{tabviz()}}.}

\item{...}{Either a single \code{PaginateSpec} / shortcut value passed
positionally (e.g. \code{paginate(x, paginate_letter())}, \code{paginate(x, 50)},
\code{paginate(x, TRUE)}, \code{paginate(x, NULL)}), or named arguments forwarded
to \code{\link[=paginate_spec]{paginate_spec()}} (e.g. \code{paginate(x, rows = 20, orphan_min = 5)}).}
}
\value{
The modified \code{WebSpec} (or widget).
}
\description{
Fluent modifier mirroring the \code{paginate = ...} argument on \code{\link[=tabviz]{tabviz()}}.
Useful at the end of a pipe when the pagination policy is decided after
the spec has been built (or when toggling a saved spec on/off without
re-running the constructor).
}
\examples{
\dontrun{
tabviz(data, label = "study") |> paginate(rows = 30)
tabviz(data, label = "study") |> paginate(paginate_letter())
tabviz(data, label = "study", paginate = TRUE) |> paginate(NULL)  # turn off
}

}
\seealso{
\code{\link[=paginate_spec]{paginate_spec()}}, \code{\link[=paginate_letter]{paginate_letter()}}, \code{\link[=paginate_a4]{paginate_a4()}},
\code{\link[=paginate_slide]{paginate_slide()}}.
}
