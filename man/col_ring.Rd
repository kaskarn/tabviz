% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classes-components.R
\name{col_ring}
\alias{col_ring}
\title{Column helper: Ring (donut) gauge}
\usage{
col_ring(
  field,
  header = NULL,
  width = NULL,
  min_value = 0,
  max_value = 1,
  color = NULL,
  thresholds = NULL,
  track_color = NULL,
  size = c("base", "sm", "lg"),
  show_label = TRUE,
  label_format = c("percent", "decimal", "integer"),
  label_decimals = 0,
  na_text = NULL,
  ...
)
}
\arguments{
\item{field}{Field name containing the numeric value to gauge.}

\item{header}{Column header (default NULL, uses field name).}

\item{width}{Column width in pixels (NULL for auto-sizing).}

\item{min_value, max_value}{Numeric scalars defining the input range.
Defaults: \code{0} and \code{1} (a fraction). The donut fills
\code{(value - min_value) / (max_value - min_value)} of the full circle.}

\item{color}{Filled arc color. \code{NULL} (default) → theme accent for
single-color rendering, or the threshold-default palette when
\code{thresholds} is set. A character vector pairs with \code{thresholds}
(\code{length(color) == length(thresholds) + 1}).}

\item{thresholds}{Numeric vector of breakpoints in raw input units.
The arc color shifts when the value crosses each breakpoint.}

\item{track_color}{Unfilled track color. \code{NULL} (default) → theme muted.}

\item{size}{Ring size: \code{"sm"} (~18px), \code{"base"} (~24px, default), or
\code{"lg"} (~32px). Larger sizes may exceed normal \code{rowHeight} — best in
tall rows that already contain a \verb{viz_*} column.}

\item{show_label}{Show the numeric value centered to the right of the
ring (default \code{TRUE}).}

\item{label_format}{\code{"percent"} (default), \code{"decimal"}, or \code{"integer"}.}

\item{label_decimals}{Number of decimal places shown in the label
(default 0).}

\item{na_text}{Text to display for NA/missing values (default NULL = blank).}

\item{...}{Additional arguments passed to \code{web_col()}. \code{style_muted}
dims the entire cell; \code{style_color} overrides the filled arc color.}
}
\value{
A ColumnSpec object with type = "ring".
}
\description{
Render a small circular donut per row with a centered numeric label.
Optionally shifts color through threshold breakpoints (e.g. green safe
/ amber watch / red danger). Designed for diagnostic dashboards where
"how full" needs to read at a glance.
}
\details{
Theme-aware defaults: when \code{color} and \code{track_color} are \code{NULL}, the
donut uses the active theme's accent and muted slots. Threshold
defaults follow the package status palette:
\itemize{
\item 1 threshold → \code{c(accent, status_negative)}
\item 2 thresholds → \code{c(status_positive, status_warning, status_negative)}
\item 3+ thresholds → require an explicit \code{color} vector
}

Thresholds are interpreted in the \strong{raw input units} (not normalized),
matching the convention used by \code{\link[=col_pvalue]{col_pvalue()}}.
}
\examples{
# Single-color ring, value as fraction [0, 1]
col_ring("temptation", color = "#d4a955")

# Two-color threshold (gold below 50\%, red above)
col_ring("temptation",
         color = c("#d4a955", "#a855f7"), thresholds = 0.5)

# Three-color status: positive / warning / negative
col_ring("risk", thresholds = c(0.33, 0.66))

# Integer score [0, 100]
col_ring("score", min_value = 0, max_value = 100,
         label_format = "integer")
}
