% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dtcg.R
\name{suggest_theme}
\alias{suggest_theme}
\title{Suggest a theme from a single brand color.}
\usage{
suggest_theme(
  brand,
  polarity = "light",
  accent = "complementary",
  name = "suggested"
)
}
\arguments{
\item{brand}{Brand color as a hex string (e.g. \code{"#0066cc"}).}

\item{polarity}{\code{"light"} (default) or \code{"dark"}.}

\item{accent}{Accent strategy: \code{"complementary"} (default), \code{"analogous"},
or \code{"mono"}.}

\item{name}{Theme name to stamp (default \code{"suggested"}).}
}
\value{
A resolved \link{WebTheme}.
}
\description{
The brand-bootstrap flagship: derive a complete, contrast-safe theme from
one hex color. Paper / ink / accent are derived from the brand hue and the
cascade's own min-contrast guarantees keep the result legible — no manual
tuning. Ideal for "make me a theme from our brand color" (incl. LLM
callers).
}
\examples{
\dontrun{
suggest_theme("#0066cc")
suggest_theme("#7a1f3d", polarity = "dark", accent = "analogous")
}
}
\seealso{
\code{\link[=theme_to_dtcg]{theme_to_dtcg()}}, \code{\link[=web_theme]{web_theme()}}
}
