% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paginate.R
\name{paginate_spec}
\alias{paginate_spec}
\title{Configure pagination for a tabviz}
\usage{
paginate_spec(
  rows = 30L,
  break_on = c("split", "group", "none"),
  keep_groups = TRUE,
  orphan_min = 3L,
  repeat_header = TRUE,
  repeat_legend = TRUE,
  repeat_title = TRUE,
  footnotes_on = c("last", "every"),
  page_label = "x_of_y",
  oversized_group_policy = c("overflow", "warn", "error")
)
}
\arguments{
\item{rows}{Maximum data rows per page (positive integer). Default 30,
tuned for US letter portrait at typical font sizes.}

\item{break_on}{When forced page breaks should occur. One of:
\itemize{
\item \code{"split"} (default): every \code{split_by} subview starts on a new page.
\item \code{"group"}: every group starts on a new page (group integrity always
respected when \code{keep_groups = TRUE}).
\item \code{"none"}: no forced breaks; rows pack continuously.
}}

\item{keep_groups}{If \code{TRUE} (default), never break in the middle of a
group. If a group plus the current page would exceed \code{rows}, the group
moves to the next page.}

\item{orphan_min}{Minimum rows on a trailing page. If the last page has
fewer rows than this, rows are pulled back from the prior page. Default
3; set 0 to disable.}

\item{repeat_header, repeat_legend, repeat_title}{Whether the column header,
forest legend, and plot title repeat on every page in PDF export.
Default \code{TRUE} for all three. Ignored by the HTML viewer (which always
shows these once at the top of the viewport).}

\item{footnotes_on}{Where footnotes/source notes appear: \code{"last"}
(default — only on the final page) or \code{"every"} (every page).}

\item{page_label}{How the "Page X of Y" indicator is rendered. One of:
\code{TRUE} (default style), \code{FALSE} (hide), \code{"x"} (just \code{1}, \code{2}, ...),
\code{"x_of_y"} (default — \verb{1 of 5}), or a function \verb{function(page, total)}
that returns a character string.}

\item{oversized_group_policy}{What to do when a single group has more rows
than \code{rows}: \code{"overflow"} (default — emit on its own page, possibly
taller than budget; \code{cli::cli_inform()} lists the offenders),
\code{"warn"} (same plus \code{cli::cli_warn()}), \code{"error"} (abort).}
}
\value{
A \code{PaginateSpec} S7 object.
}
\description{
Attach a \code{PaginateSpec} to a \code{\link[=tabviz]{tabviz()}} (via \code{paginate = ...}) to break a
long table into multiple pages. The HTML viewer renders one page at a time
with prev/next controls; PDF export emits one PDF page per logical page.
Single-image formats (PNG, SVG) warn and flatten by default — pass an
explicit spec to \code{\link[=save_plot]{save_plot()}} to override per-render.
}
\details{
Breakpoints are computed once on the R side from the data and group
structure, so the viewer and the PDF export agree on where each page
starts and ends.
}
\examples{
# Default 30 rows per page
paginate_spec()

# Smaller pages with strict orphan control
paginate_spec(rows = 20, orphan_min = 5)

# No forced break per split_by subview, no group integrity
paginate_spec(break_on = "none", keep_groups = FALSE)

}
\seealso{
\code{\link[=paginate_letter]{paginate_letter()}}, \code{\link[=paginate_a4]{paginate_a4()}}, \code{\link[=paginate_slide]{paginate_slide()}},
\code{\link[=paginate]{paginate()}} for the fluent modifier.
}
