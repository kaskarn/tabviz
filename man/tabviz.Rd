% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/web_spec.R
\name{tabviz}
\alias{tabviz}
\alias{web_spec}
\title{Create an interactive table visualization}
\usage{
tabviz(
  data,
  label = NULL,
  label_header = "Study",
  group = NULL,
  columns = NULL,
  extra_columns = NULL,
  available_exclude = NULL,
  title = NULL,
  subtitle = NULL,
  caption = NULL,
  footnote = NULL,
  watermark = NULL,
  watermark_color = NULL,
  watermark_opacity = NULL,
  row_bold = NULL,
  row_italic = NULL,
  row_color = NULL,
  row_bg = NULL,
  row_badge = NULL,
  row_icon = NULL,
  row_indent = NULL,
  row_type = NULL,
  row_emphasis = NULL,
  row_muted = NULL,
  row_accent = NULL,
  row_fill = NULL,
  marker_color = NULL,
  marker_shape = NULL,
  marker_opacity = NULL,
  marker_size = NULL,
  weight = NULL,
  theme = web_theme_cochrane(),
  interaction = NULL,
  plot_position = NULL,
  row_height = NULL,
  banding = NULL,
  width = NULL,
  height = NULL,
  elementId = NULL,
  split_by = NULL,
  shared_axis = FALSE,
  shared_column_widths = FALSE,
  .spec_only = FALSE,
  .original_call = NULL,
  axis_range = lifecycle::deprecated(),
  axis_ticks = lifecycle::deprecated(),
  axis_gridlines = lifecycle::deprecated(),
  zoom = lifecycle::deprecated(),
  auto_fit = lifecycle::deprecated(),
  max_width = lifecycle::deprecated(),
  max_height = lifecycle::deprecated(),
  show_zoom_controls = lifecycle::deprecated()
)

web_spec(..., .spec_only = TRUE)
}
\arguments{
\item{data}{A data.frame, data.table, or tibble}

\item{label}{Column name for row labels (optional)}

\item{label_header}{Header text for the label column (default: "Study")}

\item{group}{Grouping specification. Can be:
\itemize{
\item Single string: Column name for flat grouping, e.g., \code{"category"}
\item Character vector: Column names for hierarchical nesting from outermost
to innermost, e.g., \code{c("region", "country")} creates region > country
\item List of \code{web_group()} objects for explicit control over labels and structure
}}

\item{columns}{List of column specifications (use \verb{col_*()} helpers).
Include \code{viz_forest()} to add a forest plot column.}

\item{extra_columns}{List of column specifications that are hidden by default but
available in the interactive column picker. Pre-configured formatting for optional
columns; users can surface them at runtime.}

\item{available_exclude}{Character vector of data columns to omit from the interactive
column picker (for sensitive or internal-only fields).}

\item{title}{Main title (displayed above the plot)}

\item{subtitle}{Subtitle (displayed below the title)}

\item{caption}{Caption (displayed below the plot)}

\item{footnote}{Footnote (displayed below caption, italicized)}

\item{watermark}{Optional watermark text (e.g. \code{"DRAFT"}, \code{"CONFIDENTIAL"})}

\item{watermark_color}{Optional CSS color for the watermark; inherits
\code{theme.colors.foreground} when \code{NULL}.}

\item{watermark_opacity}{Optional \verb{[0, 1]} fill-opacity for the watermark;
defaults to \code{0.07}.
rendered behind the table rows at low opacity. The text is centered over
the rows region and rotated to follow that region's diagonal, so the angle
adapts to the table's aspect ratio. Theme-aware: uses the foreground color.
\code{NULL} (default) draws no watermark.}

\item{row_bold}{Row-level bold styling. Column name (character) or formula
(e.g., \code{~ p_value < 0.05}) evaluating to logical values.}

\item{row_italic}{Row-level italic styling. Column name or formula.}

\item{row_color}{Row text color. Column name or formula returning CSS color strings.}

\item{row_bg}{Row background color. Column name or formula returning CSS color strings.}

\item{row_badge}{Label badges. Column name or formula returning text values.}

\item{row_icon}{Label icons. Column name or formula returning emoji/unicode.}

\item{row_indent}{Row indentation. Column name or formula returning numeric values.}

\item{row_type}{Row type. Column name or formula returning "data", "header", "summary", "spacer".}

\item{row_emphasis}{Emphasis styling (bold + primary fg + primary marker). Column name or formula (logical).}

\item{row_muted}{Muted styling (lighter, reduced prominence). Column name or formula (logical).}

\item{row_accent}{Accent styling (bold + accent color). Column name or formula (logical).}

\item{row_fill}{Fill styling (bold + pastel accent-derived row tint). Column name or formula (logical).}

\item{marker_color}{Marker fill color. Column name or formula returning CSS color strings.}

\item{marker_shape}{Marker shape. Column name or formula returning "square", "circle", "diamond", "triangle".}

\item{marker_opacity}{Marker opacity. Column name or formula returning numeric 0-1.}

\item{marker_size}{Marker size driver. Column name or formula returning
numeric weight-like values (e.g. study weights 0-100). Values are
normalized by the renderer via \code{0.5 + sqrt(w/100) * 1.5} and clamped so
marker areas scale sensibly — pass the raw weight column, not a
pre-normalized multiplier.}

\item{weight}{Deprecated: use marker_size instead}

\item{theme}{Theme object (use \verb{web_theme_*()} functions)}

\item{interaction}{Interaction settings (use \code{web_interaction()})}

\item{plot_position}{\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#deprecated}{\figure{lifecycle-deprecated.svg}{options: alt='[Deprecated]'}}}{\strong{[Deprecated]}} No longer has
rendering effect; column order in \code{columns = list(...)} controls
placement. Accepted for back-compat; emits a deprecation warning.}

\item{row_height}{Numeric row height in pixels}

\item{banding}{Row banding mode. One of \code{"none"}, \code{"row"}, \code{"group"} (default),
or \code{"group-n"} (e.g. \code{"group-2"} to alternate at the 2nd group level). When
\code{"group"} is used without a level, the deepest group present is used; if
there are no groups, it falls back to row-level banding. Overrides the
banding setting on the supplied \code{theme}.}

\item{width}{Widget width (default NULL for auto)}

\item{height}{Widget height (default NULL for auto)}

\item{elementId}{HTML element ID (optional)}

\item{split_by}{Column name(s) to split data into separate plots with sidebar navigation.
Creates a SplitForest with one plot per unique value (or combination of values).}

\item{shared_axis}{When \code{split_by} is used, whether to use the same axis range
across all split plots. Default is \code{FALSE}.}

\item{shared_column_widths}{When \code{split_by} is used, whether every sub-plot
shares identical per-column widths. Default \code{FALSE}. Set \code{TRUE} when you
plan to stack screenshots (slides / PowerPoint) and want columns to line
up. Widths are computed once from the combined data.}

\item{.spec_only}{If TRUE, return the WebSpec object instead of rendering.}

\item{.original_call}{Internal. Overrides the \code{match.call()} capture used
by the "View source" panel. Wrapping entry points (e.g. \code{forest_plot()})
pass their own deparsed call so the emitted R chain reflects the user's
actual top-level call rather than the forwarded \code{tabviz(...)} shape.
Useful for programmatic manipulation before rendering.}

\item{axis_range}{\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#deprecated}{\figure{lifecycle-deprecated.svg}{options: alt='[Deprecated]'}}}{\strong{[Deprecated]}} Pass \code{axis_range}
to \code{viz_forest()} on the forest column instead.}

\item{axis_ticks}{\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#deprecated}{\figure{lifecycle-deprecated.svg}{options: alt='[Deprecated]'}}}{\strong{[Deprecated]}} Pass \code{axis_ticks}
to \code{viz_forest()} on the forest column instead.}

\item{axis_gridlines}{\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#deprecated}{\figure{lifecycle-deprecated.svg}{options: alt='[Deprecated]'}}}{\strong{[Deprecated]}} Pass
\code{axis_gridlines} to \code{viz_forest()} on the forest column instead.}

\item{zoom}{\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#deprecated}{\figure{lifecycle-deprecated.svg}{options: alt='[Deprecated]'}}}{\strong{[Deprecated]}} Pipe to
\code{set_zoom(zoom = ...)} after constructing the widget.}

\item{auto_fit}{\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#deprecated}{\figure{lifecycle-deprecated.svg}{options: alt='[Deprecated]'}}}{\strong{[Deprecated]}} Pipe to
\code{set_zoom(auto_fit = ...)} after constructing the widget.}

\item{max_width}{\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#deprecated}{\figure{lifecycle-deprecated.svg}{options: alt='[Deprecated]'}}}{\strong{[Deprecated]}} Pipe to
\code{set_zoom(max_width = ...)} after constructing the widget.}

\item{max_height}{\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#deprecated}{\figure{lifecycle-deprecated.svg}{options: alt='[Deprecated]'}}}{\strong{[Deprecated]}} Pipe to
\code{set_zoom(max_height = ...)} after constructing the widget.}

\item{show_zoom_controls}{\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#deprecated}{\figure{lifecycle-deprecated.svg}{options: alt='[Deprecated]'}}}{\strong{[Deprecated]}} Pipe to
\code{set_zoom(show_controls = ...)} after constructing the widget.}

\item{...}{Arguments passed to \code{tabviz()}}
}
\value{
An htmlwidget object, or a WebSpec object if \code{.spec_only = TRUE}
}
\description{
\code{tabviz()} is the main entry point for creating interactive table visualizations.
It returns an htmlwidget that can be displayed in R Markdown, Quarto, Shiny, or
the RStudio viewer.
}
\details{
Use \verb{col_*()} helpers in the \code{columns} argument to define what to display:
\itemize{
\item \code{viz_forest()} for forest plot intervals (point + CI)
\item \code{col_text()}, \code{col_numeric()}, \code{col_pvalue()} for formatted values
\item \code{col_bar()}, \code{col_sparkline()} for inline visualizations
\item \code{col_badge()}, \code{col_icon()}, \code{col_stars()} for categorical displays
}

For multi-effect forest plots, pass a list of \code{effect_forest()} objects to \code{viz_forest()}.
}
\examples{
\dontrun{
# Basic forest plot - returns interactive widget
data <- data.frame(
  study = c("A", "B", "C"),
  estimate = c(1.2, 0.8, 1.5),
  ci_lo = c(0.9, 0.5, 1.1),
  ci_hi = c(1.6, 1.2, 2.0),
  n = c(100, 150, 200)
)

tabviz(
  data,
  label = "study",
  columns = list(
    col_text("study"),
    viz_forest(point = "estimate", lower = "ci_lo", upper = "ci_hi"),
    col_numeric("n", header = "N")
  )
)

# Log scale for odds ratios
tabviz(
  data,
  label = "study",
  columns = list(
    viz_forest(
      point = "estimate", lower = "ci_lo", upper = "ci_hi",
      scale = "log", null_value = 1, axis_label = "Odds Ratio"
    )
  )
)

# Get the spec object for manipulation
spec <- tabviz(data, label = "study",
  columns = list(viz_forest(point = "estimate", lower = "ci_lo", upper = "ci_hi")),
  .spec_only = TRUE
)
print(spec)
as.data.frame(spec)
}

}
