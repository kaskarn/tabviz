% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classes-theme.R
\name{web_font}
\alias{web_font}
\title{Construct a webfont entry for a WebTheme}
\usage{
web_font(family, url)
}
\arguments{
\item{family}{Display family name (e.g. \code{"Cinzel"}). Used for
documentation and is the same string that goes into the theme's
font stack.}

\item{url}{The full stylesheet URL to load. Must start with
\code{"https://"} (Google Fonts, BunnyFonts, jsDelivr, etc.).}
}
\value{
A list with two named entries (\code{family}, \code{url}).
}
\description{
Companion helper for the \code{web_fonts} slot on \code{\link[=web_theme]{web_theme()}}. Returns a
list suitable for inclusion in the theme's \code{web_fonts = list(...)}
argument. The frontend appends one \verb{<link rel="stylesheet">} per
entry to \code{document.head} on widget mount (deduped by URL across
multiple widgets on a page).
}
\details{
Theme authors are still responsible for referencing the loaded family
in \code{font_body} / \code{font_display} (etc.). \code{web_font()} only declares the
load — it doesn't change the theme's font stacks.

Note: PNG/SVG export through \code{rsvg} does not fetch webfonts. The
system fallback stack will be used. For high-fidelity offline export,
install the font locally on the rendering machine.
}
\examples{
web_font("Cinzel", "https://fonts.googleapis.com/css2?family=Cinzel:wght@400;700&display=swap")
}
