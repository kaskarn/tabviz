% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conditions.R
\name{condition}
\alias{condition}
\title{Define a named row predicate}
\usage{
condition(name, rule, label = NULL, category = NULL)
}
\arguments{
\item{name}{A short identifier used to refer to the condition.
Conventional snake_case ("significant", "is_high_risk").}

\item{rule}{A formula or function that produces a logical vector of
length \code{nrow(data)}. Formulas evaluate against the data frame
(e.g. \code{~ p_value < 0.05}); functions receive a row's metadata
list and the full rows list.}

\item{label}{Human-readable label for editor / legend display.
Defaults to \code{name}.}

\item{category}{Optional grouping hint for the editor.}
}
\value{
A \code{tabviz_condition} object — a list with class attribute,
carrying \code{name}, \code{rule}, \code{label}, \code{category}. The rule is
\emph{un-evaluated} at this point; \code{tabviz()} materializes it against
the data.
}
\description{
Conditions are pre-computed boolean vectors evaluated against the
table data once at spec construction. Other columns can reference
them by name via \code{\link[=cond]{cond()}} in style arguments (e.g.
\code{bold = cond("significant")}), so a single rule drives styling
across multiple cells / columns without copy-paste.
}
\examples{
\dontrun{
tabviz(
  data        = meta,
  conditions  = list(
    condition("significant", ~ p_value < 0.05)
  ),
  columns     = list(
    col_text("study", bold = cond("significant"))
  )
)
}
}
