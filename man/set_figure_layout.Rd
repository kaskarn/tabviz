% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modifiers.R
\name{set_figure_layout}
\alias{set_figure_layout}
\title{Attach interactive figure-layout state to a spec}
\usage{
set_figure_layout(
  x,
  column_widths = NULL,
  row_kind_heights = NULL,
  column_order = NULL
)
}
\arguments{
\item{x}{A \code{WebSpec} object or htmlwidget.}

\item{column_widths}{Named list/vector of column width pins
(column id → px). Accepts the raw Shiny input
(\verb{input$<id>_column_widths} — the envelope is unwrapped automatically).
Widths pin like a user drag: they survive re-measure, theme and
density changes.}

\item{row_kind_heights}{Named list/vector of row-kind height pins
(kind → px). Accepts the raw Shiny input
(\verb{input$<id>_row_kind_heights}). Kinds: \code{"data"}, \code{"group_header"},
\code{"spacer"}, \code{"summary"}, \code{"header"}.}

\item{column_order}{Reorder: either a flat character vector of column
ids (the shape \verb{input$<id>_column_order} emits — applied at top
level), or \verb{list(top_level = c("id1", ...), by_group = list(<group id> = c(...)))}. Unknown ids are inert
(dropped at apply time).}
}
\value{
The modified WebSpec or htmlwidget (invisibly).
}
\description{
The figure-layout block carries the layout adjustments a user found
interactively — column width pins, column reorder, and per-row-kind
height pins — as part of the spec wire
(interactivity-UX arc P1; \code{docs/dev/interactivity-ux-plan.md}). The
canonical use is a Shiny round-trip: read the widget's emitted state and
re-attach it when re-rendering, so a data refresh keeps the user's
layout.
}
\details{
The widget hydrates this block UNDER live session state — an adjustment
the user makes after render always wins over the block's value for the
same column/kind.
}
\examples{
\dontrun{
# Shiny: persist the user's layout across a data refresh
output$plot <- render_tabviz({
  tabviz(filtered_data(), label = "study", columns = cols) |>
    set_figure_layout(
      column_widths = isolate(input$plot_column_widths),
      row_kind_heights = isolate(input$plot_row_kind_heights)
    )
})
}

}
