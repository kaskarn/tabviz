% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/column-schema.R
\name{column_schema}
\alias{column_schema}
\title{Describe a column type's option contract}
\usage{
column_schema(type)
}
\arguments{
\item{type}{A column type string, e.g. \code{"pvalue"}, \code{"numeric"}, \code{"bar"}.
One of \code{\link[=list_column_types]{list_column_types()}}\verb{$type}.}
}
\value{
A data frame with one row per option: \code{option}, \code{kind}
(\code{core}/\code{styling}/\code{presentation}), \code{themeable} (logical), \code{control}, \code{default},
\code{choices} (comma-separated valid values, or \code{NA}), \code{hint}, and
\code{inherited_from} (the schema in the chain that declares it).
}
\description{
Returns the full set of options a column type accepts — resolved across the
schema inheritance chain — with each option's \code{kind}, default, valid
choices, and whether a theme may default it. This is the machine-readable
contract behind \verb{col_<type>()}: use it to discover option names/values, or
to see exactly which options a theme's \code{column_defaults} (via
\code{\link[=set_column_default]{set_column_default()}}) is allowed to touch (\code{themeable == TRUE}, i.e.
\code{kind} is \code{"styling"} or \code{"presentation"}; \code{"core"} options change what the data
means and are never theme-defaultable).
}
\examples{
\dontrun{
column_schema("pvalue")
# just the theme-defaultable ones:
subset(column_schema("pvalue"), themeable)
}
}
\seealso{
\code{\link[=list_column_types]{list_column_types()}}, \code{\link[=set_column_default]{set_column_default()}}
}
