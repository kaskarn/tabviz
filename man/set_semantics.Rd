% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classes-theme.R
\name{set_semantics}
\alias{set_semantics}
\title{Modify theme semantic bundles}
\usage{
set_semantics(theme, emphasis = NULL, muted = NULL, accent = NULL)
}
\arguments{
\item{theme}{A \link{WebTheme} object.}

\item{emphasis}{Named list of \link{SemanticBundle} fields to update on the
emphasis bundle. Supplying \code{NULL} leaves the token untouched.}

\item{muted}{Same for the muted bundle.}

\item{accent}{Same for the accent bundle.}
}
\value{
The modified \code{WebTheme}.
}
\description{
Pipe-friendly function to customize the visual bundles behind the
\code{emphasis} / \code{muted} / \code{accent} semantic classes. Each argument accepts a
named list with any subset of \code{fg}, \code{bg}, \code{border}, \code{marker_fill},
\code{font_weight}, \code{font_style} — omitted fields preserve the theme's existing
values for that token (so you can tweak a single field without restating
the rest of the bundle).
}
\examples{
web_theme_default() |>
  set_semantics(
    emphasis = list(fg = "#000000", bg = "#fefce8", font_weight = 700),
    accent   = list(fg = "#b91c1c", marker_fill = "#dc2626")
  )
}
