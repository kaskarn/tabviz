% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shiny.R
\name{tabviz_state_envelope}
\alias{tabviz_state_envelope}
\title{Read the full observability state, envelope intact}
\usage{
tabviz_state_envelope(input, id)
}
\arguments{
\item{input}{The Shiny \code{input} object}

\item{id}{The widget's output id (the same one passed to \code{tabvizOutput()})}
}
\value{
A named list of envelopes, each with \code{value}, \code{source}, \code{ts}.
}
\description{
Like \code{\link[=tabviz_state]{tabviz_state()}} but preserves each dimension's envelope, so callers
see \code{source} ("user" / "proxy") and \code{ts} alongside the value. Useful when
a \code{reactive()} should ignore proxy-originated mutations to avoid feedback
loops:
}
\details{
\if{html}{\out{<div class="sourceCode r">}}\preformatted{observe(\{
  sort <- input[[paste0("tbl_sort")]]
  req(sort, sort$source == "user")
  # ... react only to user-driven sort changes
\})
}\if{html}{\out{</div>}}
}
