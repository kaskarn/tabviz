% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-nse.R
\name{resolve_style_expr}
\alias{resolve_style_expr}
\title{Resolve a style expression to a column name}
\usage{
resolve_style_expr(expr, data, param_name, env = parent.frame())
}
\arguments{
\item{expr}{The styling expression: either a column name (character) or formula}

\item{data}{The data frame to evaluate against}

\item{param_name}{Name of the parameter (for error messages)}

\item{env}{Environment for formula evaluation (default: caller's environment)}
}
\value{
A list with:
\itemize{
\item \code{col_name}: The column name to use (either original or computed)
\item \code{data}: The (possibly modified) data frame with computed column added
}
}
\description{
Handles both column name strings and formula expressions for styling.
When a formula is provided, evaluates it against the data and creates
a computed column with the results.
}
\examples{
\dontrun{
df <- data.frame(p = c(0.01, 0.1, 0.001))

# Column name
resolve_style_expr("is_sig", df, "row_bold")

# Formula
resolve_style_expr(~ p < 0.05, df, "row_bold")
}
}
\keyword{internal}
