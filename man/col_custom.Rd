% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classes-components.R
\name{col_custom}
\alias{col_custom}
\title{Author a column of a JS-registered plugin type}
\usage{
col_custom(field, type, header = NULL, width = NULL, options = list(), ...)
}
\arguments{
\item{field}{Data column name.}

\item{type}{The registered plugin type string (must match the JS
\code{registerColumnType()} key).}

\item{header}{Column header. Defaults to the field name.}

\item{width}{Column width in pixels, or \code{"auto"}.}

\item{options}{A named list shipped verbatim as the column's
\code{options} payload. Namespace it the way the plugin's schema
expects (typically \verb{list(<type> = list(...))}).}

\item{...}{Standard style-mapping arguments (\code{bold}, \code{color},
\code{tooltip}, ...), same as every \verb{col_*()} helper.}
}
\value{
A \code{ColumnSpec}.
}
\description{
The TS engine's \code{registerColumnType()} (\verb{@tabviz/core/extend}) lets
JavaScript authors add new column types at runtime. \code{col_custom()} is
the R escape hatch for those types: it deliberately bypasses the
built-in type roster and ships \code{type} + \code{options} to the wire
untouched, where the JS-side registered schema and renderers pick
them up by type match. R cannot validate a plugin's options — that is
the plugin's contract.
}
\details{
For the built-in types, use the dedicated \verb{col_*()} helpers, which
validate their arguments.
}
\examples{
# Consuming a JS plugin registered as "fancy_ring":
col_custom("score", type = "fancy_ring", header = "Score",
           options = list(fancy_ring = list(max = 10)))
}
