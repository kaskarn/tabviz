% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modifiers.R
\name{selectable_themes}
\alias{selectable_themes}
\title{Curate which themes appear in the interactive theme switcher}
\usage{
selectable_themes(x, themes)
}
\arguments{
\item{x}{A \code{WebSpec} or htmlwidget created by \code{\link[=tabviz]{tabviz()}}.}

\item{themes}{One of:
\itemize{
\item \code{NULL}: hide the switcher entirely.
\item \code{"default"}: show all \code{\link[=package_themes]{package_themes()}}.
\item A list of \code{WebTheme} objects: show exactly those. Named list entries
override each theme's displayed name
(e.g. \code{list(Classical = web_theme_jama(), Modern = web_theme_modern())}).
}}
}
\value{
The modified WebSpec or htmlwidget.
}
\description{
Fluent sibling to the \code{enable_themes} argument of \code{\link[=web_interaction]{web_interaction()}}.
Rewrites \code{spec@interaction@enable_themes} on a WebSpec or htmlwidget so the
switcher menu shows only the requested themes. The spec's active theme is
always included automatically, so the user can always revert.
}
\examples{
\dontrun{
tabviz(data, label = "study") |>
  set_theme("jama") |>
  selectable_themes(list(
    Classical = web_theme_jama(),
    Modern = web_theme_modern()
  ))
}
}
