% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/themes-api.R
\name{tabviz_theme_css}
\alias{tabviz_theme_css}
\title{Emit a resolved theme as CSS custom properties.}
\usage{
tabviz_theme_css(theme)
}
\arguments{
\item{theme}{A \link{WebTheme} (resolved or unresolved; re-resolved at call
time if needed), or a \link{WebSpec} whose theme is used.}
}
\value{
A character(1) string of \verb{--tv-*: value;} declarations.
}
\description{
Returns the portable \verb{--tv-*} CSS custom-property block the widget applies
to its root container. Useful for inspection, exporting a theme to
surrounding chrome (e.g. a Quarto report that wants matching colors), or
building tooling around theme drafts.
}
\details{
Delegates to the TS \code{getThemeCSS()} (\verb{@tabviz/core}) via V8 so R and JS
produce byte-identical output by construction — no parallel CSS emission.
R↔TS parity is guarded by \code{tests/testthat/test-parity-themes.R}.
}
\examples{
\dontrun{
  css <- tabviz_theme_css(web_theme_cochrane())
  cat(css)
}
}
