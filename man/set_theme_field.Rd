% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/themes-api.R
\name{set_theme_field}
\alias{set_theme_field}
\title{Set a single theme field by path (generic deep setter).}
\usage{
set_theme_field(theme, path, value)
}
\arguments{
\item{theme}{A \link{WebTheme}.}

\item{path}{Character (or mixed character/integer) vector from the theme
root to the target leaf.}

\item{value}{New value for the leaf.}
}
\value{
The \link{WebTheme} with the field set (re-resolved when \code{path} targets
an input).
}
\description{
Escape hatch for pinning any leaf in the resolved theme tree without a
dedicated setter. \code{path} is a character vector of property names from the
theme root to the leaf; integer entries index list properties (e.g.
\code{series}). Assigning under \code{inputs} re-resolves the cascade (the change is
an input); any other path is a post-resolution pin applied directly.
}
\examples{
\dontrun{
  web_theme_cochrane() |> set_theme_field(c("row_group", "L1", "bg"), "#EEE")
  web_theme_cochrane() |> set_theme_field(c("series", 1L, "fill"), "#FF0000")
}
}
