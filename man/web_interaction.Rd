% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classes-components.R
\name{web_interaction}
\alias{web_interaction}
\alias{web_interaction_minimal}
\alias{web_interaction_publication}
\alias{web_interaction_full}
\title{Create interaction specification}
\usage{
web_interaction(
  show_legend = NA,
  enable_sort = NA,
  enable_collapse = NA,
  enable_select = NA,
  enable_hover = NA,
  enable_resize = NA,
  enable_export = NA,
  enable_filters = NA,
  enable_reorder_rows = NA,
  enable_reorder_columns = NA,
  enable_edit = NA,
  enable_theme_edit = NA,
  enable_axis_zoom = NA,
  show_group_counts = NA,
  tooltip_fields = NULL,
  enable_themes = getOption("tabviz.enable_themes", "default"),
  show_filters = lifecycle::deprecated()
)

web_interaction_minimal()

web_interaction_publication()

web_interaction_full()
}
\arguments{
\item{show_legend}{Show legend}

\item{enable_sort}{Enable column sorting}

\item{enable_collapse}{Enable group collapsing}

\item{enable_select}{Enable row selection}

\item{enable_hover}{Enable hover effects}

\item{enable_resize}{Enable column resizing}

\item{enable_export}{Enable download/export button}

\item{enable_filters}{Enable per-column filter popovers in column headers.}

\item{enable_reorder_rows}{Enable drag-and-drop reordering of rows (within a group)
and row-groups (among siblings). Session-only; exported SVG/PNG reflects reorders.}

\item{enable_reorder_columns}{Enable drag-and-drop reordering of columns (within a
column group) and column-groups (among top-level siblings).}

\item{enable_edit}{Enable double-click inline editing for text/numeric/label cells
plus a popover editor for forest-cell numerics.}

\item{enable_theme_edit}{Enable the in-widget theme settings cog (Tier-1 theme
edits + per-figure controls). \code{FALSE} removes the cog for an author-frozen
figure.}

\item{enable_axis_zoom}{Enable x-axis domain zoom on forest/viz columns
(Ctrl/Cmd+wheel to zoom, drag to pan, double-click to reset). Default
\code{FALSE}: domain zoom changes what the figure shows, so it is opt-in.}

\item{show_group_counts}{Show \code{(n)} after each row-group label. Default \code{FALSE}.}

\item{tooltip_fields}{Character vector of column names to show in hover tooltip (NULL = no tooltip)}

\item{enable_themes}{Control theme selection menu:
\itemize{
\item \code{"default"} (default): Enable theme menu with all \code{package_themes()}
\item \code{NULL}: Disable theme selection entirely (hide menu icon)
\item A list of WebTheme objects: Enable theme menu with only the specified themes
}}

\item{show_filters}{\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#deprecated}{\figure{lifecycle-deprecated.svg}{options: alt='[Deprecated]'}}}{\strong{[Deprecated]}} Use \code{enable_filters} instead.}
}
\value{
An InteractionSpec object
}
\description{
Two prefixes appear in this spec by design:
\itemize{
\item \strong{\verb{show_*}} controls whether a piece of UI chrome is rendered at all
(e.g. \code{show_legend}, \code{show_group_counts}). Toggles visibility of static
elements; nothing the user does at runtime brings them back.
\item \strong{\verb{enable_*}} controls whether a user \emph{capability} is available
(sorting, filtering, resizing, editing, reordering, exporting, the theme
menu). The associated UI is rendered when the capability is on, hidden
when off.
}
}
\details{
Use this distinction when adding new arguments: render-or-not is \code{show_},
can-the-user-do-it is \code{enable_}.

Every flag defaults to \code{NA} = "not specified". Unspecified flags resolve
through a defaults chain (explicit setting > theme opinion
(\code{web_theme(interaction_defaults=)}) > global default
(\code{options(tabviz.interaction_defaults=)}) > baked defaults). The baked
defaults are conservative: reader-safe capabilities (sorting, collapsing,
hover, selection, filters, export, column resize, the settings cog) are
ON; author-grade affordances that change what the figure shows (inline
editing, row/column reorder, axis domain zoom) are OFF. Pass explicit
\code{TRUE}/\code{FALSE} to override any tier; \code{\link[=web_interaction_full]{web_interaction_full()}} switches
everything on explicitly.
}
