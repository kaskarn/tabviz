% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dimensions.R
\name{tabviz_natural_dimensions}
\alias{tabviz_natural_dimensions}
\title{Natural dimensions of a tabviz spec}
\usage{
tabviz_natural_dimensions(x)
}
\arguments{
\item{x}{A \code{WebSpec} object (e.g. from \code{tabviz(..., .spec_only = TRUE)})
or an htmlwidget produced by \code{tabviz()} / \code{forest_plot()}.}
}
\value{
A named list with three numeric scalars: \code{width}, \code{height},
\code{aspect} (= \code{width / height}). All in logical pixels.
}
\description{
Compute the spec's natural width, height, and aspect ratio without
rendering an SVG. The natural width is the sum of column widths plus the
theme's chrome (padding, dividers); the natural height is row count times
the theme's row height plus header / axis / footer chrome. The aspect
ratio is \code{width / height}.
}
\details{
This is the dimension \code{save_plot()} would render at when called with no
\code{width}, \code{height}, or \code{ratio} arguments. Authors who want to size a
figure chunk to match a tabviz can pre-compute the natural aspect and
set \code{fig.height} accordingly.
}
\examples{
\dontrun{
spec <- tabviz(meta_data, label = "study",
               columns = list(viz_forest("hr", "lower", "upper")),
               .spec_only = TRUE)
dims <- tabviz_natural_dimensions(spec)
dims$width    # e.g. 800
dims$aspect   # e.g. 1.04

# Sizing a Quarto chunk:
#| fig-width: 8
#| fig-height: !expr 8 / dims$aspect
}

}
