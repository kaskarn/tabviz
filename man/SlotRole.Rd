% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classes-theme.R
\name{SlotRole}
\alias{SlotRole}
\title{SlotRole: 7-field per-slot role bundle for series and summary.}
\description{
Resolution fills NA fields from the slot's anchor via OKLCH:
fill      <- anchor
stroke    <- darken(anchor)
fill_dim  <- mix(anchor, surface.base)       (de-emphasized state)
stroke_dim<- darken(fill_dim)
fill_hot  <- darken(anchor) + chroma boost   (interactive highlight)
stroke_hot<- darken(anchor) further
text_fg   <- contrast-checked against typical row bg
}
\details{
Renamed from \code{SlotBundle} (Sprint 1 PR 2). The fields previously
called \code{fill_muted} / \code{stroke_muted} / \code{fill_emphasis} /
\code{stroke_emphasis} are now \code{fill_dim} / \code{stroke_dim} / \code{fill_hot} /
\code{stroke_hot} — \code{muted} and \code{emphasis} each carried multiple
meanings across the theme system; \code{dim}/\code{hot} reserves them for the
chrome roles and paint tokens.
}
